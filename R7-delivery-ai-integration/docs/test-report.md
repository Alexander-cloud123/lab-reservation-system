# R7 轮次测试报告 —— AI 模块（Agnes 接入与降级容错）

## 一、测试环境与前置

| 项 | 值 |
| --- | --- |
| 操作系统 | Windows（PowerShell 5.1） |
| 后端 | Spring Boot 3.2.10 / JDK 21 / MyBatis-Plus 3.5.7，运行于 http://localhost:8080 |
| 前端 | Vue 3.4.38 / Vite 5.4.21 / Element Plus 2.7.8 / ECharts 5.5.1 / FullCalendar 6.1.15，运行于 http://localhost:5173 |
| 数据库 | Docker MySQL 8.0（reservation-mysql，库 reservation，root/root，utf8mb4） |
| 基线 | R1-R6 共 210 条既有用例（T01-T212）+ R7 新增用例（T213-T241，按运行模式分支）= 最多 241 条 |
| 测试脚本 | `docs/test-script.ps1`（复用 R6 脚本并追加 R7 段，可重复执行，结束后恢复初始基线） |
| 初始基线 | 用户 5 / 教室 12 / 预约 13（待审核3·已通过6·已驳回2·已取消2）/ 收藏 6 / ai_config 5（ai.enable=false，compliance_keywords/prompt_parse/prompt_compliance 基线值未改动） |

## 二、总结果

### 运行模式 1：交付配置（ai.enable=false，双开关关闭）

```
用例总数: 228   通过: 228   失败: 0
```

- = R1-R6 210 条基线零回归 + R7 关闭路径/权限/边界/只读 18 条（T213-T230）。

### 运行模式 2：联调模式（ai.enable=true，双开关开启；无 AGNES_API_KEY 走降级规则）

```
用例总数: 234   通过: 234   失败: 0
```

- = 开启路径分支执行 T231-T241（11 条）+ 关闭路径专属用例 T220-T224 自动跳过（5 条），净新增 6 条。

### 浏览器实测（Chrome，http://localhost:5173）

- 学生端：AI 推荐卡 Top3（含推荐理由、「AI 生成，仅供参考」标注）→ 点击直达教室详情 ✅；AI 快速预约完整流程（自然语言描述 → 结构化解析预填 → 按类型/容量过滤选教室 → 确认提交 → 前后端双重冲突校验 →「预约提交成功，等待管理员审核」）✅；全局悬浮助手问答（相关问题命中 FAQ、无关问题返回场景外预设话术）✅。
- 管理端：预约审核页待审核记录显示「AI 校验：通过」标签（绿色 success）✅；临时将用途改为违规词后显示「AI 校验：违规」（红色 danger 高亮 + 用途红色）+ 悬浮显示原因（「包含违规关键词：商业推销」）✅；只提示不改状态，审核动作仍为管理员手动执行 ✅。
- 交付配置下（ai.enable=false）：学生端推荐卡/快速预约按钮/悬浮球全部隐藏，管理端无 AI 校验标签，系统完全退化为 R6 纯预约系统 ✅。

### 降级机制实测（全部通过，HTTP 200 无 500、不阻断业务）

| 场景 | 实测结果 |
| --- | --- |
| 无密钥（AGNES_API_KEY 未配置） | message=「AI 服务未配置密钥，已自动切换为本地规则模式」，规则结果可用 |
| 限流（RPM=20 固定窗口） | 连续调用第 21 次（窗口计数 21 > 20）触发，message=「AI 请求过于频繁，已自动切换为本地规则模式」，answer 仍返回规则 FAQ；日志同步 WARN |
| 超时/连接失败（base-url 指向不可达地址 + 假密钥） | 66ms 内返回 message=「AI 服务暂时不可用，已自动切换为本地规则模式」，parse/compliance 均正常降级出规则结果 |
| 服务异常（假密钥 4xx/5xx） | 与连接失败同路径（RestClientException 统一捕获 → 降级），无 500 |

## 三、R1-R6 基线回归（T01-T212，共 210 条，零回归失败）

| 轮次 | 用例数 | 结果 |
| --- | --- | --- |
| R1 基础（认证/注册/统一返回/全局异常） | 21 | 全部通过 |
| R2 用户管理 + 教室管理 | 80 | 全部通过 |
| R3 预约核心（冲突检测/提交/审核/取消/批量/我的预约） | 54 | 全部通过 |
| R4 体验升级（个人中心/收藏/体验细节） | 28 | 全部通过 |
| R5 亮点功能（日历区间/看板统计） | 27 | 全部通过 |
| R6 联调优化（预约记录页/导出/字段完整性） | 46 | 全部通过 |

关键回归锚点：冲突检测四类边界、状态流转 0/1/2/3、开始前 1 小时取消限制、统计口径与库一致、日历登录即可 / stats 管理员权限、列表与详情接口不含 password、预约记录字段完整性、导出 xlsx 合法文件头。

## 四、R7 新增用例明细（T213-T241）

### 1. 权限校验（T213-T219，7 条）

| 编号 | 用例名称 | 类别 | 结果 |
| --- | --- | --- | --- |
| T213-T216 | 4 个 AI 接口未登录访问 | 权限校验 | 401 ✅ |
| T217 | AI 推荐-学生 Token 可访问（登录即可） | 权限校验 | 200 ✅ |
| T218 | AI 合规-管理员 Token 可访问（登录即可） | 权限校验 | 200 ✅ |
| T219 | AI 问答-伪造 Token 访问 | 权限校验 | 401 ✅ |

### 2. 边界与异常（T225-T229，5 条）

| 编号 | 用例名称 | 类别 | 结果 |
| --- | --- | --- | --- |
| T225 | AI 推荐-缺 userId | 边界场景 | 友好处理（非 500）✅ |
| T226 | AI 解析-缺 text | 边界场景 | 友好处理（非 500）✅ |
| T227 | AI 问答-缺 question | 边界场景 | 友好处理（非 500）✅ |
| T228 | AI 合规-缺 purpose | 边界场景 | 友好处理（非 500）✅ |
| T229 | AI 问答-非法 JSON | 异常操作 | 400 ✅ |

### 3. 只读校验（T230，1 条）

| 编号 | 用例名称 | 类别 | 结果 |
| --- | --- | --- | --- |
| T230 | 4 接口调用前后 zhangsan 预约数不变 | 数据安全 | before=4 after=4 ✅ |

### 4. 开关与双开关（关闭路径 T220-T224 / 开启路径 T241）

| 编号 | 用例名称 | 类别 | 运行模式 | 结果 |
| --- | --- | --- | --- | --- |
| T220 | AI 推荐-关闭路径 enabled=false 友好提示 | 开关 | 关闭路径 | enabled=False + message=「AI 服务未启用，当前为纯预约系统模式」✅ |
| T221-T223 | 解析/问答/合规-关闭路径 enabled=false | 开关 | 关闭路径 | enabled=False ✅ |
| T224 | 双开关 AND（yml 关 + db 开 = 关） | 开关 | 关闭路径 | enabled=False ✅ |
| T241 | 双开关 AND（yml 开 + db 关 = 关） | 开关 | 开启路径 | enabled=False ✅ |

### 5. 开启路径功能与降级（T231-T240，联调模式执行）

| 编号 | 用例名称 | 类别 | 结果 |
| --- | --- | --- | --- |
| T231 | AI 推荐-返回 Top3 含教室与理由 | 正常流程 | count=3 ok=True ✅ |
| T232 | AI 解析-结构化字段正确（date 格式 / 14:00-16:00 / 40 人 / 机房） | 正常流程 | ok=True ✅ |
| T233 | AI 问答-相关问题返回 FAQ | 正常流程 | len=82 ok=True ✅ |
| T234 | AI 问答-场景外预设话术（场景绝对限定） | 正常流程 | scoped=True ✅ |
| T235 | AI 合规-违规词判定（compliant=False + reason） | 正常流程 | ok=True ✅ |
| T236 | AI 合规-正常用途通过（compliant=True） | 正常流程 | ok=True ✅ |
| T237 | AI 问答-无密钥自动降级本地规则 | 降级 | msg=「AI 服务未配置密钥，已自动切换为本地规则模式」✅ |
| T238 | AI 问答-限流自动降级（RPM=20，25 次循环至少 1 次命中） | 降级 | hit=True msg=「AI 请求过于频繁…」✅ |
| T239 | AI 推荐-响应时间 ≤ 3s（非功能需求） | 正常流程 | ms=15 ✅ |
| T240 | AI 推荐-响应不含 password | 数据安全 | noPwd=True ✅ |

> 注：T238 限流断言采用「循环 25 次至少 1 次命中」而非「恰好第 21 次」——固定窗口计数在并发下窗口内瞬时可越过 limit（RPM≈20 机制仍有效），避免并发抖动导致的偶发失败。

## 五、联调发现并修复的问题

| # | 问题 | 修复 | 验证 |
| --- | --- | --- | --- |
| 1 | recommend 接口 500：`AiFallbackEngine.rankAndTop` 对 `stream().toList()` 不可变列表调 sort → `UnsupportedOperationException` | 改为 `.collect(Collectors.toList())` | recommend 实测 53ms 返回 Top3+理由 |
| 2 | 降级原因不可观测：限流/无密钥/超时静默返回规则结果，测试无法断言 | 4 个 ServiceImpl 降级分支写 `vo.setMessage(resp.reason())` | T237/T238 及无密钥/超时实测均透出降级原因 |
| 3 | R6 基线用例 T155 时间依赖：晚间 20 点后「2 小时后」预约越过营业时间边界（22:00）创建失败 | 脚本内适配：超边界时改用明日 8:00（仍在 24h「即将开始」窗口内） | 修复后 T155 ok=True（resId=191） |

## 六、已知遗留（不影响验收，记录备查）

- 限流固定窗口计数在并发下窗口内瞬时可越过 limit（第 21/22 次调用可能同时 ok=false），RPM≈20 机制实测有效；测试断言已按「25 次内至少 1 次命中」设计。

## 七、数据库基线复核（脚本清理段执行后）

```
reservation=13（待审核3/已通过6/已驳回2/已取消2）  user_favorite=6  sys_user=5  ai_config=5（ai_enable=false，基线值未改动）
```

- 测试脚本可复跑验证：交付配置首轮 228/228、联调模式 234/234、修复后交付配置复跑 228/228；每次运行后数据库均恢复初始基线。
