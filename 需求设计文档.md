# 实验室/教室预约管理系统 — 需求分析与设计说明书 V3.1（AI 赋能冲优版）

> **版本记录**
> - V1.0：核心业务闭环（13 页面、双重冲突检测、数据看板）
> - V2.0：人性化体验升级（个人中心升级、全流程体验细节、管理端效率优化）
> - V3.0：轻量场景化 AI 能力接入（低耦合、可插拔、不占核心周期）
> - **V3.1（当前）**：大模型服务商确定为 **Agnes AI**（兼容 OpenAI 规范、文本模型永久免费、无限额度）；技术栈整体适配 **JDK 21 + Node 22 + IDEA 内置 Maven 3.9.x** 本地环境（Spring Boot 3.2 LTS）
>
> **项目选题**：《基于双重校验机制的高校实验室预约管理系统设计与实现》
> （备选：《面向师生双端的高校教室预约服务系统设计与实现》）
>
> **开发模式**：基于完整顶层设计的迭代式增量开发，全程由豆包 Agents 执行编码，项目负责人仅做每轮最终验收。

---

## 一、系统需求分析

### 1.1 系统定位与设计理念
本系统针对高校实验室/教室预约线下流程繁琐、冲突频发、找房效率低、审核工作量大的痛点，实现预约全流程数字化管理。

- **基础底线**：核心业务逻辑严谨，预约冲突检测精准（前后端双重校验），权限划分清晰，满足实训全部考核点。
- **体验升级**：从师生双视角出发，补充场景化细节体验，让系统从「能用」升级为「好用」。
- **AI 差异化拔高**：引入大模型能力赋能核心业务场景，不做无关通用聊天，聚焦「提升预约效率、降低审核成本」两个核心价值，用轻量化 AI 能力打造代差级亮点。
- **核心定位**：AI 赋能的智能化教室资源调度系统（技术 + 体验双维度差异化，冲刺优秀评级）。

### 1.2 角色定义
| 角色 | 权限范围 | 账号来源 | 核心设计视角 |
|------|----------|----------|--------------|
| 学生 | 浏览教室、提交预约、管理个人预约、收藏常用教室、查看个人数据、AI 辅助预约 | 自主注册 | 降低操作成本，快速找到可用教室，及时获知预约状态 |
| 管理员 | 教室资源管理、预约审核、用户管理、全量记录查询、数据统计、AI 辅助审核 | 系统预置 | 提升审核效率，批量处理操作，数据直观可览 |

### 1.3 功能需求清单（13 页完整版，分层标注）

#### 公共功能（2 页）
| 功能点 | 性质 | 说明 |
|--------|------|------|
| 双角色登录 | 基础 | 学生/管理员角色切换登录，表单校验，错误提示 |
| 学生自主注册 | 基础 | 信息填写、唯一性校验、密码加密存储 |
| 登录状态提醒 | 冲优 | 当日有即将开始的预约，登录后顶部温和提示 |

#### 学生端功能（5 页）
| 功能点 | 性质 | 说明 |
|--------|------|------|
| 教室资源列表浏览 | 基础 | 多条件筛选、关键词搜索、卡片式展示、分页 |
| 教室实时状态标签 | 冲优 | 卡片直接显示「当前空闲/使用中/今日剩余 X 时段」 |
| 筛选条件记忆 | 冲优 | 自动保留上次选择的楼栋、类型、日期筛选条件 |
| **AI 智能教室推荐** | AI 亮点 | 基于用户历史预约习惯（时段、楼栋、类型、人数）+ 实时空闲状态，智能匹配 Top3 最合适的教室 |
| **AI 自然语言快速预约** | AI 亮点 | 口语化描述预约需求，自动解析日期、时段、人数、类型、用途，一键匹配可用教室发起预约 |
| 教室详情查看 | 基础 | 基础信息展示、当日时段占用状态可视化 |
| 教室收藏 | 冲优 | 常用教室一键收藏，快速访问（上限 10 间） |
| 预约申请 | 基础 | 选择日期时段、填写用途、提交前冲突检测 |
| 预约草稿自动保存 | 冲优 | 表单未提交关闭弹窗，下次打开自动填充（localStorage） |
| 我的预约管理 | 基础 | 按状态分类查看、取消预约 |
| 今日预约置顶 | 冲优 | 当日预约在列表顶部置顶突出显示 |
| 友好空状态 | 冲优 | 无数据时展示引导文案 + 快捷操作按钮 |
| 个人中心 | 基础 | 个人信息修改、密码修改 |
| 个人预约数据概览 | 冲优 | 累计预约次数、本月预约数、审核通过率等数据卡片 |
| 常用教室快捷入口 | 冲优 | 展示收藏的教室，点击直达预约 |
| 消息通知中心 | 冲优 | 集中展示审核结果、预约提醒类通知（由预约表状态动态生成，已读状态存 localStorage） |
| **AI 预约智能助手** | AI 亮点 | 全局侧边悬浮助手，仅解答预约、教室、个人记录相关问题，支持自然语言查询 |
| 预约日历总览 | 亮点功能 | 月/周视图日历（FullCalendar），色块展示占用情况，点击快速发起预约 |

#### 管理员端功能（6 页）
| 功能点 | 性质 | 说明 |
|--------|------|------|
| 首页数据概览 | 基础 | 核心数据卡片（今日预约、待审核、教室总数、用户总数）、快捷功能入口 |
| 教室资源管理 | 基础 | 增删改查、状态管理、批量操作、多条件搜索 |
| 预约审核 | 基础 | 全量申请查询、一键通过/驳回、驳回备注 |
| 批量审核 | 冲优 | 勾选多条待审核记录，一键批量通过/批量驳回（仅待审核状态可参与） |
| 快捷驳回原因 | 冲优 | 预置常用驳回原因（时间冲突、用途不明确、教室维护、人数超限），一键选择 |
| **AI 预约合规校验** | AI 亮点 | 自动校验预约用途合规性，命中违规内容自动标记并提示原因，辅助管理员审核 |
| 用户管理 | 基础 | 账号查询、禁用/启用、重置密码 |
| 预约记录查询 | 基础 | 多条件筛选、历史全量记录、Excel 导出（EasyExcel） |
| 数据可视化看板 | 亮点功能 | 教室使用率排行柱状图、月度预约趋势折线图、热门时段分布饼图（ECharts） |

### 1.4 核心业务规则

#### 基础规则（高分核心，禁止简化）
1. **预约冲突检测规则（前后端双重校验）**
   同一教室、同一预约日期下，新预约时间段与**已通过**预约时间段存在重叠，即判定为冲突，禁止提交。
   - 重叠判定公式：`新预约开始时间 < 已有预约结束时间 AND 新预约结束时间 > 已有预约开始时间`
   - 前端：选择时段后实时校验，冲突则禁用提交并提示
   - 后端：提交接口二次校验，防止绕过前端直接调用

2. **预约状态流转规则**
   - `待审核(0)` → 管理员通过 → `已通过(1)` → 时段结束后自动完成
   - `待审核(0)` → 管理员驳回 → `已驳回(2)`（不可修改）
   - `待审核(0)/已通过(1)` → 用户主动取消 → `已取消(3)`

3. **补充业务规则**
   - 取消预约时限：预约开始前 1 小时可自由取消；不足 1 小时不可取消（需管理员特殊处理）
   - 常用教室上限：每人最多收藏 10 间教室
   - 批量审核约束：仅「待审核」状态的预约可参与批量操作

#### AI 业务规则（Agents 严格遵守）
1. **场景绝对限定**：所有 AI 能力仅服务于教室预约业务场景，禁止回答任何无关话题，禁止闲聊。
2. **只读不写原则**：AI 仅可查询数据、解析需求、给出建议，**绝对不允许直接执行业务操作**（取消预约、通过审核等），所有操作必须由用户手动确认执行。
3. **降级容错机制**：大模型调用超时（3s）、报错、限流、额度异常时，自动降级为本地规则模拟模式，不阻断正常业务流程。
4. **结果可质疑**：AI 生成的所有结果必须提供「修改/重新选择」入口，不强制用户接受。
5. **内容标注**：所有 AI 生成内容，前端必须标注「AI 生成，仅供参考」。

### 1.5 非功能需求
- 响应时间：普通页面加载 ≤2s，冲突检测查询 ≤500ms，AI 接口响应 ≤3s
- 兼容性：兼容 Chrome、Edge 主流浏览器
- 安全性：密码 BCrypt 加密存储、接口权限拦截、参数合法性校验、API 密钥加密配置（仅后端持有）
- 易用性：核心操作流程不超过 3 步，关键操作均有二次确认

---

## 二、系统总体设计

### 2.1 架构设计（新增 AI 能力层）
采用标准前后端分离 B/S 架构，AI 模块独立封装，与核心业务逻辑低耦合、可插拔。

```
浏览器 → Vue前端应用（AI交互组件） → HTTP请求 → SpringBoot接口层
                                                          ↓
                                      核心业务逻辑层 ← 独立AI能力层 → Agnes AI 开放接口
                                                          ↓
                                                   MySQL数据库
```

### 2.2 最终技术栈（适配 JDK 21 + Node 22 + IDEA 内置 Maven 3.9.x）

| 层级 | 技术选型 | 版本 | 说明 |
|------|----------|------|------|
| 开发环境 | JDK | 21 LTS | 本地现有版本，官方长期支持版 |
| 构建工具 | Maven | 3.9.x（IDEA 内置） | 无需单独安装，Runner JRE 选 Project SDK |
| 后端核心框架 | Spring Boot | 3.2.10 LTS | 官方正式支持 JDK 21 |
| ORM 框架 | MyBatis-Plus | 3.5.7（mybatis-plus-spring-boot3-starter） | 适配 Spring Boot 3 |
| 接口文档 | Knife4j | 4.4.0（knife4j-openapi3-jakarta-spring-boot-starter） | OpenAPI 3 规范 |
| Excel 导出 | EasyExcel | 3.4.0 | 兼容高版本 JDK |
| 工具库 | Hutool | 5.8.32 | 通用工具（含 HTTP 客户端） |
| Lombok | Lombok | 1.18.34 | 支持 JDK 21 |
| 数据库 | MySQL | 8.0.x | Docker 容器方式运行（本项目专用容器 reservation-mysql，映射 3306:3306） |
| 前端运行环境 | Node.js | 22.x | 本地现有版本，LTS |
| 前端框架 | Vue 3 + Vite | 3.4.x / 5.x | 完美兼容 Node 22 |
| UI 组件库 | Element Plus | 2.7.x | 后台系统首选 |
| 状态管理 | Pinia | 2.x | Vue3 官方推荐 |
| 路由 | Vue Router | 4.x | 路由管理 + 权限守卫 |
| 亮点插件 | ECharts 5 / FullCalendar 6 | 5.5.x / 6.1.x | 数据看板 / 日历视图 |
| 工具库 | day.js | 1.11.x | 轻量时间处理 |
| **AI 服务商** | **Agnes AI** | agnes-2.0-flash（默认）/ agnes-2.5-pro-beta（进阶） | 兼容 OpenAI v1 接口规范，文本模型永久免费、无 Token 额度上限，仅限 RPM≈20 次/分钟 |
| AI 接入节点 | 国内节点 | https://apihub.agnes-ai.cn/v1 | 国内访问稳定、延迟低（国际站：https://apihub.agnes-ai.com/v1） |
| 可选加分 | Redis | 7.0.x | Docker 容器方式运行（reservation-redis，映射 6379:6379），缓存热门教室数据、存储登录 Token（须经负责人确认） |

> **版本选型原则说明（答辩用）**：稳定优先、兼容优先、避坑优先。JDK 21 + Spring Boot 3.2 为当前官方 LTS 主流组合；前端 Vue3 + Vite5 完整支持 Node 22；包名统一使用 `jakarta.*`（Spring Boot 3 迁移点）。

### 2.3 数据库设计

#### 2.3.1 核心 E-R 关系
- 用户表 ← 一对多 → 预约表
- 教室表 ← 一对多 → 预约表
- 用户表 ← 多对多 → 教室表（通过收藏关联表）
- AI 功能不新增核心业务表，仅新增 1 张轻量配置表，不改动原有核心表结构

#### 2.3.2 表结构定义

##### 1. 用户表 `sys_user`
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | bigint | PRIMARY KEY, AUTO_INCREMENT | 主键 ID |
| username | varchar(32) | UNIQUE, NOT NULL | 登录账号 |
| password | varchar(64) | NOT NULL | 密码（BCrypt 加密） |
| name | varchar(20) | NOT NULL | 真实姓名 |
| student_no | varchar(20) | NULL | 学号（学生角色必填） |
| phone | varchar(11) | NULL | 联系电话 |
| email | varchar(50) | NULL | 邮箱 |
| role | tinyint | NOT NULL, DEFAULT 0 | 角色：0-学生，1-管理员 |
| status | tinyint | NOT NULL, DEFAULT 1 | 状态：0-禁用，1-正常 |
| create_time | datetime | NOT NULL | 创建时间 |
| update_time | datetime | NOT NULL | 更新时间 |

##### 2. 教室表 `classroom`
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | bigint | PRIMARY KEY, AUTO_INCREMENT | 主键 ID |
| name | varchar(50) | NOT NULL | 教室名称 |
| building | varchar(30) | NOT NULL | 所属楼栋 |
| room_no | varchar(20) | NOT NULL | 教室编号 |
| type | tinyint | NOT NULL | 类型：1-普通教室，2-实验室，3-机房 |
| capacity | int | NOT NULL | 容纳人数 |
| equipment | text | NULL | 设备说明 |
| description | varchar(255) | NULL | 备注描述 |
| status | tinyint | NOT NULL, DEFAULT 1 | 状态：0-停用，1-可用 |
| create_time | datetime | NOT NULL | 创建时间 |
| update_time | datetime | NOT NULL | 更新时间 |

##### 3. 预约表 `reservation`
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | bigint | PRIMARY KEY, AUTO_INCREMENT | 主键 ID |
| user_id | bigint | NOT NULL, INDEX | 预约用户 ID |
| classroom_id | bigint | NOT NULL, INDEX | 预约教室 ID |
| reserve_date | date | NOT NULL, INDEX | 预约日期 |
| start_time | time | NOT NULL | 开始时间 |
| end_time | time | NOT NULL | 结束时间 |
| purpose | varchar(255) | NOT NULL | 预约用途 |
| status | tinyint | NOT NULL, DEFAULT 0 | 状态：0-待审核，1-已通过，2-已驳回，3-已取消 |
| audit_remark | varchar(255) | NULL | 审核备注 |
| auditor_id | bigint | NULL | 审核人 ID |
| audit_time | datetime | NULL | 审核时间 |
| create_time | datetime | NOT NULL | 创建时间 |
| update_time | datetime | NOT NULL | 更新时间 |

##### 4. 用户收藏表 `user_favorite`
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | bigint | PRIMARY KEY, AUTO_INCREMENT | 主键 ID |
| user_id | bigint | NOT NULL, INDEX | 用户 ID |
| classroom_id | bigint | NOT NULL, INDEX | 教室 ID |
| create_time | datetime | NOT NULL | 收藏时间 |

##### 5. AI 配置表 `ai_config`
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | bigint | PRIMARY KEY, AUTO_INCREMENT | 主键 ID |
| config_key | varchar(50) | UNIQUE, NOT NULL | 配置键 |
| config_value | text | NOT NULL | 配置值 |
| description | varchar(255) | NULL | 配置说明 |
| create_time | datetime | NOT NULL | 创建时间 |
| update_time | datetime | NOT NULL | 更新时间 |

> 用途：存储合规关键词、Prompt 模板等配置，避免硬编码，可动态调整。

#### 2.3.3 索引设计
- `reservation`：联合索引 `idx_class_date(classroom_id, reserve_date)`，优化冲突检测查询
- `user_favorite`：联合唯一索引 `idx_user_class(user_id, classroom_id)`，防止重复收藏

### 2.4 前端页面详细设计（13 页总量不变）

#### 公共页面（2 个）
1. **登录页**：账号、密码、角色单选（学生/管理员）、登录按钮、注册入口。登录后若有当日即将开始的预约，顶部温和提醒。
2. **注册页**：学号、姓名、手机号、登录账号、密码、确认密码。两次密码一致性校验、账号唯一性校验。

#### 学生端页面（5 个）
3. **教室列表页（学生首页）**：搜索栏（关键词/楼栋/类型/日期筛选）+ 教室卡片列表 + 分页；卡片显示实时状态标签（空闲绿/使用中红/剩余时段灰）；筛选条件记忆；顶部「AI 为你推荐」卡片区（Top3）；搜索栏旁「AI 快速预约」入口。
4. **教室详情页**：基础信息卡片、当日时段占用状态可视化、收藏/取消收藏按钮、预约申请按钮（弹窗表单）。
5. **我的预约页**：状态标签页（待审核/已通过/已驳回/已取消）、预约记录列表、取消按钮（二次确认）；今日预约置顶；友好空状态。
6. **个人中心页**：顶部个人预约数据概览卡片（累计预约、本月预约、通过率、最近一次）；常用教室快捷入口；消息通知列表；个人信息表单；修改密码表单（验证原密码）。
7. **预约日历总览页（亮点）**：FullCalendar 月/周视图切换、预约色块标记、点击日期快速发起预约、悬浮显示详情。

> **全局悬浮组件：AI 预约助手**（不占页面）——学生端所有页面右下角悬浮球，点击展开侧边对话窗口，仅解答预约/教室/操作相关问题，当前会话不持久化。

#### 管理员端页面（6 个）
8. **后台首页**：数据统计卡片（今日预约、待审核、教室总数、用户总数）+ 快捷功能入口。
9. **教室资源管理页**：搜索栏、数据表格、新增/编辑（弹窗）/删除（二次确认）、状态开关、批量禁用/启用。
10. **预约审核管理页**：状态筛选、预约记录表格、通过/驳回按钮（二次确认）；复选框批量审核；驳回弹窗预置快捷原因；待审核记录增加「AI 校验」标签，违规红色高亮并悬浮显示原因。
11. **用户管理页**：搜索栏、用户列表、禁用/启用、重置密码（二次确认）。
12. **预约记录页**：多条件筛选（日期、教室、用户、状态）、记录列表、Excel 导出。
13. **数据看板页（亮点）**：ECharts 三图表（教室使用率排行柱状图、月度预约趋势折线图、热门时段分布饼图），支持时间范围筛选。

### 2.5 接口模块划分（5 大模块，RESTful 风格）

1. **用户模块**：登录、注册、获取当前用户信息、修改个人信息、修改密码、用户列表、用户管理、个人预约数据统计
2. **教室模块**：教室列表查询、教室详情、教室增删改查、收藏/取消收藏、我的收藏列表
3. **预约模块**：提交预约、取消预约、我的预约列表、预约审核、全量预约查询、冲突检测、批量审核
4. **统计模块**：首页概览数据、教室使用率统计、预约趋势统计、时段分布统计
5. **AI 能力模块**（独立，不侵入业务）：
   - `POST /api/ai/recommend`：智能教室推荐（输入用户 ID → 推荐教室列表 Top3）
   - `POST /api/ai/parse-reservation`：自然语言预约解析（输入文本 → 结构化参数：日期、时段、人数、类型、用途）
   - `POST /api/ai/chat`：预约智能问答（输入问题 → 限定场景回答）
   - `POST /api/ai/compliance-check`：预约合规校验（输入用途 → 合规结果、违规原因）

---

## 三、面向豆包 Agents 编程的开发规划

### 3.1 工程目录结构
#### 后端 `reservation-server`
```
reservation-server
├── src/main/java/com/example/reservation
│   ├── common          # 统一返回 Result、全局异常、配置类、常量类
│   ├── entity          # 数据库实体类
│   ├── mapper          # DAO 层接口
│   ├── service         # 业务层接口 + impl 实现类
│   ├── controller      # 控制层接口
│   ├── ai              # AI 能力独立包（service/dto/config）
│   └── ReservationApplication.java
├── src/main/resources
│   ├── application.yml
│   └── mapper
└── pom.xml
```
#### 前端 `reservation-web`
```
reservation-web
├── src
│   ├── api             # 接口请求封装
│   ├── assets          # 静态资源
│   ├── components
│   │   ├── common      # 公共组件
│   │   └── ai          # AI 交互组件（助手、推荐卡、自然语言输入框）
│   ├── router          # 路由配置 + 权限守卫
│   ├── stores          # Pinia 状态管理
│   ├── utils           # 工具函数（请求、时间处理）
│   ├── views
│   │   ├── common      # 登录、注册页
│   │   ├── student     # 学生端 5 页
│   │   └── admin       # 管理员端 6 页
│   ├── App.vue
│   └── main.js
└── package.json
```

### 3.2 统一代码规范
1. **命名规范**：后端类名大驼峰、方法/变量小驼峰、数据库表/字段下划线；前端组件大驼峰、文件短横线命名、方法/变量小驼峰
2. **统一返回格式**：所有接口返回 `Result`：`{ "code": 200, "message": "操作成功", "data": {} }`
3. **注释规范**：类、接口、核心方法必须加功能注释；冲突检测等复杂逻辑加行内注释并标注规则依据
4. **包名规范**：后端统一 `jakarta.*`，禁止出现 `javax.*`
5. **禁止硬编码**：魔法值必须定义为常量类；密钥只存配置/环境变量

### 3.3 8 周迭代计划（AI 后置，核心优先）

| 周数 | 阶段 | 任务单元 | 交付物 | 负责人验收重点 |
|------|------|----------|--------|----------------|
| 第 1 周 | 准备与基础框架 | 数据库建表+测试数据；SpringBoot 3.2 工程；Vue3+Vite 工程；登录注册权限 | 建表 SQL、前后端骨架、登录注册可运行 | 表结构一致、项目能启动、权限拦截生效 |
| 第 2 周 | 管理端基础 | 用户管理模块；教室资源管理模块 | 两大模块完整 CRUD + 联调 | 增删改查完整、权限正确、规范统一 |
| 第 3 周 | 学生端基础 | 教室列表/详情页；预约提交；收藏功能 | 学生端浏览/预约流程、状态标签、筛选记忆 | 核心流程跑通、体验细节生效 |
| 第 4 周 | 核心业务逻辑 | 冲突检测；审核流程；批量审核+快捷驳回 | 状态流转闭环、双重校验、审核功能 | 冲突检测准确（边界）、状态流转正确、批量生效 |
| 第 5 周 | 个人中心与联调 | 个人中心页；全量体验细节；前后端全流程联调 | 个人中心三大升级、体验优化点、学生端闭环 | 个人中心完整、体验到位、全流程通畅 |
| 第 6 周 | 两大亮点 | 日历总览页；数据看板页 | 日历页、看板页、统计接口 | 数据准确、视觉效果达标、13 页全部完成 |
| 第 7 周 | AI 模块开发 | 后端 AI 封装；前端 AI 交互；联调；降级测试 | 4 个 AI 接口、3 个 AI 场景、测试报告 | 标准场景正常、降级生效、业务边界合规 |
| 第 8 周 | 答辩准备 | 实训报告；答辩 PPT；演示脚本+测试数据；AI 演示脚本与兜底 | 完整交付材料、流畅演示流程 | 全流程顺畅、亮点突出 |

### 3.4 AI 模块专属开发约束
1. **密钥安全**：Agnes API 密钥仅配置在 `application.yml` 或环境变量，禁止硬编码，禁止前端接触。
2. **低耦合**：AI 代码独立于 `ai` 包，通过依赖注入调用业务数据，不侵入原有 service/controller。
3. **超时控制**：大模型调用超时 3 秒，超时/报错/限流立即触发降级（本地规则模拟模式），不阻断业务。
4. **Prompt 限定**：所有 Prompt 明确限定场景与结构化输出格式，禁止开放式 Prompt。
5. **可开关**：全局配置项 `ai.enable` 一键启停所有 AI 功能，关闭后系统完全退化为纯预约管理系统。
6. **内容标注**：AI 生成内容前端统一标注「AI 生成，仅供参考」。
7. **调用保护**：实现调用计数与限流保护（RPM≈20），触发限流返回友好提示。

---

## 四、冲优秀答辩核心策略

1. **立意拔高**：开场讲「传统预约系统停留在信息录入+状态管理的工具层面，本项目在严谨核心业务逻辑基础上，针对学生找房慢、管理员审核繁两个痛点，引入场景化 AI 能力，把系统从管理工具升级为智能化资源调度服务」。
2. **演示走闭环，AI 做压轴**：先演示核心业务（学生预约→冲突检测→管理员审核→数据看板）证明基础扎实，再演示 AI 亮点（自然语言预约→智能推荐→合规校验→智能助手），层层递进。
3. **重点讲两个维度**：技术维度（前后端双重冲突检测、AI 模块低耦合可插拔、降级容错机制）；产品维度（场景化 AI 不做通用功能、只解决真实业务痛点）。
4. **兜底预案**：准备无网络/API 不可用的规则模拟兜底版本，确保演示零翻车。
5. **版本选型话术**：JDK 21 + Spring Boot 3.2 LTS 为当前企业技术升级主流方向，体现对技术生态的跟进与选型考量。

---

## 五、关键设计决策记录（对话结论固化）

| 决策点 | 结论 | 理由 |
|--------|------|------|
| 页面数量 | 13 页（2 公共 + 5 学生端 + 6 管理端） | 工作量匹配 8 周，避免堆砌 |
| 人性化增强 | 个人中心升级 + 全系统体验细节 + 管理端效率优化（总量不变） | 小成本换差异化，避免同质化 |
| AI 接入 | 轻量场景化真实接入 + 规则模拟兜底 | 加分项不抢核心，演示零翻车 |
| 大模型服务商 | Agnes AI（agnes-2.0-flash，国内节点） | 兼容 OpenAI 规范、永久免费无额度上限 |
| 开发方式 | 从头开发（GitHub 仅参考界面/思路） | 查重风险低、代码可控、答辩对答如流 |
| 开发策略 | 基于顶层设计的迭代式增量开发 | 质量可控、审阅成本低、贴合 8 周 |
| 版本选型 | JDK 21 + Spring Boot 3.2.10 + Node 22（本地现有） | 官方 LTS 主流组合，无需降级安装 |
| 协作模式 | 四角色多 Agent（架构师/后端/前端/测试）+ 5 步闭环 | 负责人仅做每轮验收 |
