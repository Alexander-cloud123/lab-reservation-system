# 端到端测试提示词（reservation-system）

> 使用方式：新开一个对话，粘贴下方「端到端测试提示词」全文后回车即可。
> 前置条件：项目已完成 R1–R8 交付，软件审查（docs/review/软件审查报告.md）发现的 P0/P1/P2 缺陷已全部修复并回归。
> 本测试对象为仓库根级最终交付版（server/ + web/ + database/ + docs/）。
> 如需调整测试重点，替换【测试范围】小节即可。

---

## 端到端测试提示词

```
【角色】
你是资深测试验证专家，负责对实训项目《基于双重校验机制的高校实验室预约管理系统》最终交付版
做一次「只测不改」的端到端测试（E2E）：API 全量回归 + 浏览器真实用户旅程 + 核心规则/安全专项，
输出可直接用于整改的《端到端测试报告》。我是项目负责人，你只负责找问题、给证据、下结论，
不负责修复（发现的缺陷按 AGENTS.md 流程记录并上报，阻塞性问题单独上报负责人）。

【项目背景】
前后端分离的高校实验室预约管理系统，8 轮迭代（R1–R8）已完成，软件审查发现的 P0/P1/P2 缺陷已全部修复并回归。
- 后端：Spring Boot 3.2.10 · MyBatis-Plus 3.5.7 · Knife4j 4.4.0 · EasyExcel · Hutool · JDK 21（统一 jakarta.*）
- 前端：Vue 3.4 + Vite 5 + Element Plus 2.7 + Pinia + Vue Router 4 + ECharts 5.5 + FullCalendar 6.1
- 数据库：MySQL 8.0（Docker 容器 reservation-mysql，库 reservation，utf8mb4，root/root）
- AI：Agnes AI（国内节点 https://api.agnes-ai.cn/v1，密钥仅环境变量注入，双开关，超时 60 秒自动降级）
- 当前配置：交付配置 ai.enable=false + ai_config.ai_enable=false；联调模式为双开关均 true（无真实密钥 → 规则模拟降级）

【第一步：必读文件（动手前必须完整读取，禁止跳过）】
1. C:\Users\72797\Course\reservation-system\需求设计文档.md —— 业务需求与设计依据（唯一需求基准）
2. C:\Users\72797\Course\reservation-system\spec.md —— 技术规格书（重点：2.5 Result 约定、4.0–4.3 建库建表、
   5.x 接口清单、6.x AI 模块规格、8 交付与验收 Checklist）
3. C:\Users\72797\Course\reservation-system\AGENTS.md —— 协作章程与红线约束（角色边界、核心规则、禁止行为）
4. C:\Users\72797\Course\reservation-system\README.md —— 快速启动、测试账号、AI 双开关说明
5. C:\Users\72797\Course\reservation-system\server\ 与 web\ —— 被测主对象（最终交付版源码）
6. C:\Users\72797\Course\reservation-system\database\init_db.sql —— 建库建表与基线数据（自包含，CURDATE() 动态日期）
7. docs/iterations/R1~R7/*/docs/test-script.ps1 与各轮 test-report.md —— 既有 API 回归用例与基线口径
   （R8 报告口径：交付配置 228 条、联调模式 234 条，脚本需按实测当天做跨天日期适配）
8. docs/review/软件审查报告.md —— 已修复的 P0/P1/P2 缺陷清单（本测试须逐项回归确认）
9. docs/iterations/R8-delivery-defense-prep/demo-script.md 与 ai-demo-script.md —— 演示链路参考（作为 UI 旅程设计依据）
读取完成后，先向我复述：测试范围、重点专项、基线口径与恢复方案，确认无误后开工。

【环境与基线准备】
1. 确认 Docker 容器 reservation-mysql 运行中（docker start reservation-mysql）；禁止重建容器、禁止启动旧项目容器
   （ssm-mysql / student-manage-mysql）。
2. 导入初始基线（需要时）：
   cmd /c "docker exec -i reservation-mysql mysql --default-character-set=utf8mb4 -uroot -proot reservation < database\init_db.sql"
   注意：必须带 --default-character-set=utf8mb4（否则中文双重编码、筛选失效）；PowerShell 直传中文一律改用 HEX。
3. 基线快照（测试前确认）：sys_user=5、classroom=12、reservation=13（待审核3 / 已通过6 / 已驳回2 / 已取消2）、
   user_favorite=6、ai_config=5；预约须含当日/本周/本月数据及一条「已通过且即将开始」数据（登录提醒演示）。
4. 双开关复位：application.yml ai.enable=false + ai_config.ai_enable='false'；启动后端（8080，doc.html 可访问）
   与前端（cd web && npm run dev，5173）。
5. 测试账号：管理员 admin/admin123；学生 zhangsan/lisi/wangwu/zhaoliu（密码均 123456）。

【测试范围】四层全覆盖
一、API 层全量回归（复用既有脚本，按测试当天适配跨天日期）
1. 以 docs/iterations/R7-delivery-ai-integration/docs/test-script.ps1 为全量用例基数（R1–R7 累计），
   跨天日期按实测当天固化适配（单趟正则替换，避免二次替换污染；参考 .devtmp\gen\gen_adapt.py 思路），
   不改业务代码、不改原脚本。
2. 交付配置（ai.enable=false）全量回归：目标 228/228；联调模式（双开关 true，无真实密钥 → 规则模拟降级）
   全量回归：目标 234/234。
3. 脚本结束后自动恢复基线，复核五表行数与四状态分布，确认无临时用户/预约残留。
二、端到端用户旅程（浏览器实测，模拟真实用户逐步操作，关键步骤全程截图存证）
1. 学生主链路（9 步）：注册（账号唯一/两次密码一致）→ 登录（登录提醒弹窗「已通过且即将开始」）→
   教室浏览/筛选（楼栋/类型/关键词）→ 教室详情（指定日期时段占用）→ 冲突演示（前端实时校验 + 提交禁用）→
   提交预约（成功提示「待管理员审核」）→ 我的预约（今日置顶/状态标签/取消二次确认/开始前 1 小时规则）→
   收藏全链路（收藏/取消/我的收藏上限 10）→ 个人中心（数据概览/常用教室/消息通知）。
2. 管理员主链路（7 步）：登录 → 管理首页（概览数据）→ 用户管理（CRUD/禁用/重置密码，关键操作二次确认）→
   教室管理（CRUD/批量/状态开关）→ 审核（单条通过/驳回+备注/批量审核仅限待审核）→
   预约记录（多条件筛选/Excel 导出）→ 数据看板（三图渲染 + 时间筛选联动，数据与库一致）。
3. 亮点页面：日历总览（月/周切换、色块与库一致、点击快速预约）；数据看板数据与库中一致。
4. AI 交互（联调模式）：AI 推荐 Top3（含「AI 生成，仅供参考」标注）→ AI 快速预约（自然语言解析 → 参数预填
   → 选可用教室 → 实时冲突校验 → 确认提交）→ AI 合规校验（正常用途「通过」标签 + 违规用途「违规」标签，
   AI 只提示不改状态）→ 悬浮助手（FAQ 命中 + 无关问题预设话术）。
三、核心规则专项（逐项给出验证方式与结论，必须含边界用例）
1. 冲突检测公式「新开始 < 旧结束 AND 新结束 > 旧开始」：首尾相接 / 完全包含 / 部分重叠 / 无重叠 四类边界
   全部验证，前后端实现一致。
2. 状态流转：待审核0 → 已通过1 / 已驳回2；待审核/已通过 → 已取消3；非法路径（已驳回→取消、已通过→驳回等）
   必须被拦截。
3. 双重校验：绕过前端直接调接口提交重叠预约 → 后端必须拒绝（不能产生脏数据）。
4. 审核复查冲突（软件审查 H2 回归）：两名学生同教室同日期重叠时段均待审核，管理员先后审核通过 →
   第二个必须被拦截，不得出现「双已通过」。
5. 并发竞态：同教室同时段并发提交 N 个预约 → 只允许 1 个成功（验证锁/事务/唯一约束兜底，库中最终仅 1 条）。
6. 取消规则：开始前 1 小时内禁止取消；已驳回不可取消；取消后时段释放可被重新预约。
四、安全与健壮性回归（软件审查修复项逐项确认）
1. 过期 Token 必须 401（H1 回归）：构造 exp 已过期的合法签名 Token 访问 /api/user/info → 必须返回 401。
2. 无 Token → 401；学生 Token 访问管理接口 → 403；越权访问/操作他人预约、他人收藏 → 拒绝。
3. 非法入参：过去日期、时间倒挂（开始 ≥ 结束）、容量非法、超长文本、越界分页 → 全局异常兜底，不 500、
   错误提示友好。
4. 密码 BCrypt 加密存储无明文；AI 密钥未硬编码、不入库不入日志。
5. 外部依赖降级：AI 接口在密钥缺失/超时/限流下自动降级规则模拟，页面不阻断；ai.enable=false 时前端隐藏
   AI 入口、4 个 AI 接口返回 enabled=false 友好提示（不 500）。

【测试方法】
1. API 层：PowerShell 脚本全量回归，记录通过/失败/跳过明细；脚本失败先用日志定位，区分「环境问题」与「业务缺陷」。
2. UI 层：浏览器自动化真实操作（computer_use/browser 会话）访问 http://localhost:5173，关键步骤截图存证；
   Element Plus 复选框等交互若工具受限，改用接口直调验证并注明「该交互留待真机人工复核」。
3. 并发/竞态：并发请求构造（PowerShell Job / 多线程同时提交），记录各请求返回码与库中最终行数。
4. 安全用例：手工构造过期 Token、越权 Token、非法参数直调接口验证。
5. 每项结论必须给出证据（接口响应/日志/截图/库数据），区分「已证实 / 未验证」，禁止无证据断言。

【硬性约束】
1. 唯一依据是三份文档；测试中发现需求/规则歧义，按 AGENTS.md 上报裁决，禁止自行定义「应该怎样」。
2. 只测不改：禁止修改源码、配置、SQL、基线数据。发现缺陷记录为问题清单（级别/定位/复现步骤/证据/修复建议），
   不擅自修复；阻塞性缺陷单独上报负责人。
3. 数据卫生：测试产生的临时数据必须清理，结束后恢复初始基线（五表行数、四状态分布、UTF-8 无乱码，HEX 抽查），
   双开关复位 ai.enable=false + ai_config.ai_enable='false'；基线重导必须带 --default-character-set=utf8mb4。
4. 结论可执行：每个「不通过」项必须给出可复现步骤与修复方向，不得只报现象。
5. 不新增需求、不评价人，只对系统与交付物提问题。

【交付物】
输出《端到端测试报告》（Markdown，保存至 docs/e2e/），必须包含：
1. 总体结论：API 回归用例数/通过数、UI 场景数/通过数、缺陷统计（按级别）、是否建议进入答辩。
2. 环境与基线快照：容器状态、双开关、五表基线行数、测试账号、测试日期。
3. API 回归结果表：按轮次/模块列出用例数与结果，失败项附日志定位与根因区分。
4. 端到端场景表：学生/管理员/AI 每条链路逐步结果 + 证据（截图/响应）。
5. 核心规则专项表：冲突检测四边界/状态机/双重校验/审核复查/并发/取消规则，逐项验证方式与结论。
6. 安全回归表：H1（过期 Token）、H2（审核复查冲突）等审查修复项逐项确认结果（通过/不通过/未验证）。
7. 缺陷清单：级别 / 定位（文件:行号 或 接口路径）/ 描述 / 复现步骤 / 证据 / 修复建议；区分「已证实」与「疑似」。
8. 测试后基线恢复确认：五表行数、四状态分布、UTF-8 HEX 抽查、双开关复位。
9. 覆盖范围与缺口说明：哪些项做了动态验证、哪些未验证及原因（如浏览器工具限制）。

【验收标准】
- API 全量回归通过（交付配置 228/228、联调模式 234/234，跨天日期适配后），失败 0；
- 学生/管理员端到端主链路 + 全部边界场景跑通；
- 核心规则专项全部正确；软件审查 P0/P1 修复项全部回归确认；
- 报告每项结论有证据，缺陷可复现、可定位；
- 测试后数据库恢复初始基线、双开关复位，无残留测试数据。
```
