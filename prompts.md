# prompts.md — 新对话启动提示词（按轮次复制使用）

> **使用方式**：
> 1. 每轮迭代**新开一个对话**，粘贴「总提示词模板」并替换「任务范围 + 验收标准」为对应轮次内容（见第 3 节轮次表）。
> 2. 每轮验收通过后再开下一轮新对话，保持每个对话上下文干净。
> 3. 若在对话中已粘贴过模板，只需追加轮次任务说明，无需重复粘贴环境基线。
> 4. 本项目所有文档位于 `C:\Users\72797\Course\reservation-system\`，三份依据文档：需求设计文档.md（业务）、spec.md（技术）、AGENTS.md（协作）。

---

## 1. 项目环境基线（已实测核对 2026-09-11）

| 项 | 实测值/约定 | 说明 |
|----|-------------|------|
| JDK | 21.0.11 LTS（Temurin） | 本机安装，已验证 |
| Maven | IDEA 内置 3.9.x | Runner JRE 选 Project SDK（JDK 21） |
| Node.js / npm | v22.23.2 / 10.9.8 | 本机安装，已验证 |
| npm 镜像 | https://registry.npmmirror.com | 已约定 |
| Docker | 29.7.2（Docker Desktop） | 已就绪，引擎可正常连接 |
| MySQL | Docker 容器 reservation-mysql（mysql:8.0，宿主 3306 空闲，映射 3306:3306，root 密码待确认，默认 root） | **本项目专用容器，禁止复用旧容器**（旧容器 ssm-mysql/student-manage-mysql 已停止，勿启动） |
| Redis（可选） | Docker 容器 reservation-redis（redis:7-alpine，宿主 6379 空闲，映射 6379:6379） | 仅当负责人确认接入 Redis 加分项时创建 |
| 数据库 | 库名 `reservation`，utf8mb4 | 由 R1 建库建表 |
| 后端框架 | Spring Boot 3.2.10 + MyBatis-Plus 3.5.7 + Knife4j 4.4.0 + EasyExcel 3.4.0 + Hutool 5.8.32 | 后端统一 `jakarta.*` 包 |
| 前端框架 | Vue 3.4 + Vite 5 + Element Plus 2.7 + Pinia + Vue Router 4 + ECharts 5.5 + FullCalendar 6.1 + day.js + sass | 禁止 node-sass |
| AI 服务 | Agnes AI，国内节点 `https://apihub.agnes-ai.cn/v1`，模型 agnes-2.0-flash | 密钥须由负责人提供，仅存环境变量/配置 |

---

## 2. 总提示词模板（每轮复制，替换任务部分）

```
【角色】
你是高校实训项目《基于双重校验机制的高校实验室预约管理系统》的开发 Agents 团队，
严格按照项目 AGENTS.md 协作章程分工协作（架构师/后端/前端/测试四角色），
全程独立完成开发、测试与自检，我是项目负责人，只做最终验收。

【第一步：必读文件（动手前必须全部读完，禁止跳过）】
请依次完整读取以下三个文件，它们分别是本项目唯一的行为依据：
1. C:\Users\72797\Course\reservation-system\需求设计文档.md —— 业务需求与设计依据
2. C:\Users\72797\Course\reservation-system\spec.md —— 技术开发规格书（照此执行）
3. C:\Users\72797\Course\reservation-system\AGENTS.md —— 协作章程与红线约束
读取完成后，先向我复述：你对本轮任务范围、约束、验收标准的理解，确认无误后再开工。

【环境基线】
JDK 21 LTS · IDEA 内置 Maven 3.9.x · Node.js 22.x · Docker 运行 MySQL 8.0
（reservation-mysql，映射 localhost:3306，库名 reservation，utf8mb4）·
Spring Boot 3.2.10 · MyBatis-Plus 3.5.7 · Vue 3.4 + Vite 5 · Element Plus 2.7 ·
Agnes AI（国内节点 https://apihub.agnes-ai.cn/v1，模型 agnes-2.0-flash）。
禁止要求降级安装任何环境；禁止启动旧项目 Docker 容器；后端统一 jakarta.* 包；
禁止引入 spec.md 之外的依赖。

【任务范围】
（替换为对应轮次任务说明，见「总提示词模板」下方轮次表）

【硬性约束】
1. 唯一依据是三份文档，禁止自行新增功能、修改业务规则、调整表结构。
2. 禁止简化核心逻辑：冲突检测公式「新开始 < 旧结束 AND 新结束 > 旧开始」、
   状态流转（待审核0→已通过1/已驳回2，待审核/已通过→已取消3）、前后端双重校验。
3. 所有接口统一返回 Result 结构；关键操作（删除/取消/审核/重置密码）必须二次确认。
4. 密钥禁止硬编码（环境变量/配置）；AI 模块（如涉及）必须只读不写、可开关、超时3秒自动降级。
5. 交付的必须是可直接运行验证的完整代码，禁止交付骨架、占位、伪代码。
6. 开发过程中遇到需求歧义，按 AGENTS.md 流程上报裁决，禁止自行理解修改。

【验收标准】
（替换为对应轮次验收清单）

【交付物】
按 AGENTS.md 第 5 节交付格式打包，必须包含：可运行代码 + 本轮任务说明 +
接口清单（新增接口路径/参数/返回值）+ 测试报告（正常流程/边界场景/异常操作/权限校验
四类用例及结果）+ 启动说明（环境要求、启动步骤、验证方法）。
交付前逐项自查 spec.md 第 8 节交付 Checklist，并报告每项自查结果。
```

---

## 3. 各轮任务范围与验收标准（替换用）

| 轮次 | 任务范围 | 验收标准 |
|------|----------|----------|
| **R1** | spec.md 第 7 节 R1：建库建表+测试数据；后端骨架（依赖/配置/Result/全局异常/Knife4j）；前端骨架（依赖/目录/request 封装/路由守卫）；登录注册+Token 权限拦截 | 五张表+测试数据导入成功；后端启动无错且 /doc.html 可访问；前端 dev 正常；登录/注册/未登录拦截全通 |
| **R2** | 管理端基础：用户管理（CRUD/禁用/重置密码）+ 教室管理（CRUD/批量/状态开关）前后端完整实现 | 两模块功能完整；管理员权限校验生效；代码规范符合 AGENTS 4.3 |
| **R3** | 预约核心：教室列表/详情页；预约提交+前后端双重冲突检测；我的预约+取消；审核+批量审核+快捷驳回 | 冲突检测边界用例（首尾相接/完全包含/部分重叠/无重叠）全部正确；状态流转正确；批量审核仅限待审核 |
| **R4** | 体验升级：个人中心（数据概览/常用教室/消息通知）；收藏全链路；体验细节（草稿/筛选记忆/状态标签/空状态/今日置顶/登录提醒） | 个人中心三升级点全实现；细节全部生效；学生端 5 页闭环 |
| **R5** | 亮点功能：日历总览页（FullCalendar 月/周视图+点击快速预约）；数据看板（ECharts 三图+时间筛选） | 日历数据准确、色块正确；看板图表数据与库中一致；13 页全部完成 |
| **R6** | 联调优化：全流程联调；异常/边界补全；注释完善；全量回归测试 | 学生+管理员全流程无断点；边界操作不报错；注释齐全；测试报告完整 |
| **R7** | AI 模块：Agnes 客户端封装；4 个 AI 接口+降级兜底；前端 AI 交互（助手/推荐/快速预约/合规标签） | 标准演示场景流畅；降级机制生效；只读不写合规；ai.enable=false 不影响核心 |
| **R8** | 答辩准备：实训报告；答辩 PPT；演示脚本+测试数据；AI 演示脚本+兜底 | 交付材料齐全；演示全流程顺畅 |

---

## 4. R1 完整实例（第一轮直接复制使用）

```
【角色】
你是高校实训项目《基于双重校验机制的高校实验室预约管理系统》的开发 Agents 团队，
严格按照项目 AGENTS.md 协作章程分工协作（架构师/后端/前端/测试四角色），
全程独立完成开发、测试与自检，我是项目负责人，只做最终验收。

【第一步：必读文件（动手前必须全部读完，禁止跳过）】
请依次完整读取以下三个文件，它们分别是本项目唯一的行为依据：
1. C:\Users\72797\Course\reservation-system\需求设计文档.md —— 业务需求与设计依据
2. C:\Users\72797\Course\reservation-system\spec.md —— 技术开发规格书（照此执行）
3. C:\Users\72797\Course\reservation-system\AGENTS.md —— 协作章程与红线约束
读取完成后，先向我复述：你对本轮任务范围、约束、验收标准的理解，确认无误后再开工。

【环境基线】
JDK 21 LTS · IDEA 内置 Maven 3.9.x · Node.js 22.x · Docker 运行 MySQL 8.0
（reservation-mysql，映射 localhost:3306，库名 reservation，utf8mb4，root 密码 root）·
Spring Boot 3.2.10 · MyBatis-Plus 3.5.7 · Vue 3.4 + Vite 5 · Element Plus 2.7 ·
Agnes AI（国内节点 https://apihub.agnes-ai.cn/v1，模型 agnes-2.0-flash）。
禁止要求降级安装任何环境；禁止启动旧项目 Docker 容器（ssm-mysql/student-manage-mysql）；
后端统一 jakarta.* 包；禁止引入 spec.md 之外的依赖。

【任务范围】
执行 spec.md 第 7 节 R1 轮次，完成基础底座：
1. 数据库：确认 reservation-mysql 容器运行中（未创建则按 spec.md 4.0 创建），
   按 spec.md 第 4 节建库建表（sys_user/classroom/reservation/user_favorite/ai_config 五表）
   并导入 spec.md 4.3 要求的初始测试数据（含管理员、学生、教室、四状态预约数据）。
2. 后端骨架：按 spec.md 第 2 节搭建 Spring Boot 3.2.10 工程，完成核心依赖、
   application.yml 配置、统一返回 Result、全局异常处理器、Knife4j 集成。
3. 前端骨架：按 spec.md 第 3 节创建 Vue3+Vite 工程，安装依赖、搭建目录结构、
   封装 Axios 请求（Token 注入、401 跳转、统一错误提示）、配置路由与权限守卫。
4. 登录注册：实现学生注册（账号唯一/两次密码一致）、双角色登录（学生/管理员），
   登录签发 Token，未登录访问受保护接口返回 401。

【硬性约束】
1. 唯一依据是三份文档，禁止自行新增功能、修改业务规则、调整表结构。
2. 禁止简化核心逻辑：冲突检测公式「新开始 < 旧结束 AND 新结束 > 旧开始」、
   状态流转（待审核0→已通过1/已驳回2，待审核/已通过→已取消3）、前后端双重校验。
3. 所有接口统一返回 Result 结构；关键操作（删除/取消/审核/重置密码）必须二次确认。
4. 密钥禁止硬编码；AI 模块（如涉及）必须只读不写、可开关、超时3秒自动降级。
5. 交付的必须是可直接运行验证的完整代码，禁止交付骨架、占位、伪代码。
6. 开发过程中遇到需求歧义，按 AGENTS.md 流程上报裁决，禁止自行理解修改。

【验收标准】
1. 五张表结构与 spec.md 第 4 节完全一致，测试数据导入成功（含 4 状态预约、当日数据）。
2. 后端 mvn 启动无报错，http://localhost:8080/doc.html 可正常访问接口文档。
3. 前端 npm run dev 正常启动，登录/注册页面可用。
4. 未登录访问受保护接口返回 401 并正确跳转登录页；管理员/学生角色区分正确。
5. 密码 BCrypt 加密存储，数据库中无明文。

【交付物】
按 AGENTS.md 第 5 节交付格式打包，必须包含：可运行代码 + 本轮任务说明 +
接口清单（新增接口路径/参数/返回值）+ 测试报告（正常流程/边界场景/异常操作/权限校验
四类用例及结果）+ 启动说明（环境要求、启动步骤、验证方法）。
交付前逐项自查 spec.md 第 8 节交付 Checklist，并报告每项自查结果。
```

---

## 5. 使用注意事项

1. **每轮一个对话**：验收通过后再开下一轮，不要在一个对话里连做多轮。
2. **环境未就绪项**：MySQL/Redis 容器尚未创建（端口已验证空闲），第一轮提示词中已让 Agent 按 spec.md 4.0 创建；root 密码默认 `root`，如需其他值请先改 prompts 与 spec.md。
3. **Redis 加分项**：是否接入 Redis 由负责人决定，决定前所有轮次按不接入处理。
4. **Agnes API 密钥**：负责人需提前注册获取密钥，R7 轮次开始时提供给 Agent，仅写入环境变量/配置，不进代码。
