# R10 最终交付测试报告

> 轮次：R10-delivery-final-closeout（阶段5 最终交付打包）｜测试日期：2026-09-25｜被测版本：HEAD `11862a9`（阶段5 无障碍修复）
> 口径：本报告为最终交付的**唯一测试结论**，按 AGENTS.md 第 3 节第 4 步「测试验证」的四要素组织：**测试用例 / 覆盖范围 / 通过情况 / 遗留问题**。
> 所有数字均为负责人实测结果，原样引用，未放大、未估算。

## 一、测试环境与前置

| 项 | 值 |
| --- | --- |
| 操作系统 | Windows |
| 后端 | Spring Boot 3.2.10 / JDK 21 LTS / MyBatis-Plus 3.5.7，运行于 `http://localhost:8080`（接口契约见 `docs/api-list.md`） |
| 前端 | Vue 3.4.38 / Vite 5.4.x / Element Plus 2.7.8 / ECharts 5.5.1 / FullCalendar 6.1.15 / day.js 1.11；dev server `http://localhost:5173` |
| 前端静态检查 | ESLint 9 flat config（`npm run lint` = `eslint src --max-warnings 0`） |
| E2E 框架 | Playwright（`@playwright/test`），Chromium，`web/e2e/` |
| 数据库 | MySQL 8.0.x（库 `reservation`，初始化脚本 `database/init_db.sql`） |
| 缓存/会话 | Redis 7.0.x（登录会话 `auth:token:{userId}` + 看板/教室短缓存；可一键关闭降级为纯 JWT） |
| E2E 运行方式 | 串行（`workers: 1`、`fullyParallel: false`，因所有用例共享同一 MySQL 库，并发会互相污染时段与计数断言） |
| E2E 前置 | MySQL / Redis 容器在线；后端已启动（脚本**不自动启动后端**）；前端 dev server 由 Playwright `webServer` 自动拉起（`reuseExistingServer: true`）；`globalSetup` 先探活后端登录接口 |
| E2E 端口覆盖 | `E2E_API_PORT`（默认 8080）、`E2E_API_BASE`、`E2E_WEB_PORT`（默认 5173）；8080 被占用时后端以 `--server.port=8081` 启动并设 `E2E_API_PORT=8081` |

## 二、总结果

```
npm run lint（web）      : 0 error / 0 warning
npm run build（web）     : Vite 5 构建成功，2101 modules transformed
全量 E2E（web/e2e）      : 98 passed / 98 total，耗时 1.9m，1 worker，Chromium
数据库基线（回归后实测）  : 预约 13 / 用户 5 / 教室 12 / E2E 临时教室残留 0
```

- 静态检查：`--max-warnings 0` 下零告警，阶段4 视觉改版与阶段5 无障碍修复未引入 lint 债务。
- 生产构建：Vite 5 构建通过，2101 个模块转换完成，无构建错误。
- 全量回归：98 条用例全部通过，零失败、零跳过、零重试（配置 `retries: 0`，避免重试放大真数据副作用）。
- 数据基线：回归结束后数据回到交付基线（预约 13 / 用户 5 / 教室 12），E2E 自建临时教室残留为 0，说明用例的造数与清理闭环成立。

## 三、测试用例（15 个 spec 文件，共 98 条）

| # | spec 文件 | 用例数 | 主要用例 |
| --- | --- | --- | --- |
| 1 | `web/e2e/specs/00-smoke.spec.js` | 3 | 未登录访问受保护页面 → 跳登录页并带回跳地址；学生登录 → 教室列表渲染；管理员登录 → 管理端首页 4 张数据卡 |
| 2 | `web/e2e/specs/10-auth.spec.js` | 11 | 登录空表单校验；角色切换为管理员后进入管理端；不存在账号/密码错误提示；学生正确登录；已登录访问 `/login` 被跳回角色首页（学生/管理员各 1）；注册两次密码不一致；注册账号唯一性（用 admin 触发被拒）；注册成功可用新账号登录；登录后当日 24h 内即将开始的已通过预约 → 顶部温和提醒且不阻断跳转 |
| 3 | `web/e2e/specs/11-classroom-list.spec.js` | 7 | 关键词筛选（命中/空状态/重置）；楼栋+类型组合筛选；日期筛选显示该日已通过占用；卡片展示字段（名称/编号/楼栋/容量/类型/今日剩余/设备/按钮）；分页切换；实时状态标签与今日剩余时段与接口口径一致；筛选条件记忆（离开再返回仍保留） |
| 4 | `web/e2e/specs/12-classroom-detail.spec.js` | 8 | 基础信息展示；时段占用可视化；收藏/取消收藏；收藏上限 10（第 11 间被拒并提示，结束后还原原收藏集）；预约弹窗必填校验；开始须早于结束的前端实时提示；冲突实时校验（重叠时提示并禁用提交）；成功提交预约（用途含 E2E 前缀）并在「我的预约」可见 |
| 5 | `web/e2e/specs/13-my-reservation.spec.js` | 5 | 状态标签页切换（请求按状态过滤、行状态文案一致）；列表字段完整性（教室/日期/时段/用途/状态/审核备注/操作列）；今日预约置顶并带「今日」标记；取消预约二次确认（「再想想」不生效、「确定取消」后状态变更）；空状态引导并可跳转教室列表 |
| 6 | `web/e2e/specs/14-calendar.spec.js` | 4 | 月/周视图切换与区间数据请求同步；预约色块按状态着色并展示时段与教室名；点击日期格快速预约弹窗预填日期与默认时段；教室筛选仅保留所选教室色块 |
| 7 | `web/e2e/specs/15-profile.spec.js` | 7 | 身份卡与数据概览字段与统计接口一致；快捷功能四格跳转 + 常用教室页内滚动定位；常用教室收藏卡片字段完整与空态引导；消息通知由预约状态动态生成、单条已读与全部已读生效；编辑资料预填/校验/保存同步并复原；修改密码前端校验与原密码错误拦截；修改密码成功后跳登录页且新密码可登录（临时账号） |
| 8 | `web/e2e/specs/20-admin-home.spec.js` | 3 | 4 张数据卡片渲染且数值为非负整数；卡片数值与 `/api/stats/overview` 逐字段一致；5 个快捷入口跳转正确路由 |
| 9 | `web/e2e/specs/21-classroom-manage.spec.js` | 9 | 列表渲染 + 关键词搜索；多条件搜索（关键词/楼栋/类型）；新增必填校验与成功；容量数值被夹在 `[1, 10000]`；编辑表单回显与保存生效；启用/停用切换（自建 E2E 教室）；批量启用/停用；删除自建教室（二次确认）；存在预约记录的 A101 删除被拒并有提示 |
| 10 | `web/e2e/specs/22-audit-manage.spec.js` | 5 | 全量申请查询与筛选（状态/日期范围/关键词）；一键通过后状态变更；驳回备注必填校验 + 快捷原因回填；批量通过仅待审核生效；批量驳回时已通过记录不参与且不被改动 |
| 11 | `web/e2e/specs/23-user-manage.spec.js` | 4 | 按账号关键词查询；禁用/启用切换（禁用后无法登录、启用后恢复）；重置密码为默认 `123456`；分页翻页与每页条数切换 |
| 12 | `web/e2e/specs/24-reservation-record.spec.js` | 2 | 多条件筛选（状态 + 日期范围 + 教室 + 关键词）；Excel 导出按筛选条件触发下载且文件名为 xlsx |
| 13 | `web/e2e/specs/25-dashboard.spec.js` | 4 | 三个图表容器均渲染出 canvas；时间范围切换（近 7/30/90 天、本年）；自定义日期范围覆盖快捷范围选中态；刷新按钮重新请求并保持三图渲染 |
| 14 | `web/e2e/specs/30-ai.spec.js` | 6 | 教室列表 AI 推荐卡 Top3 可直达详情且提供重新选择入口；AI 快速预约解析回填 → 选教室 → 取消不写库；AI 悬浮助手默认收起 → 展开对话窗 → 回答渲染与「AI 生成」标注；管理端 AI 合规校验显示结果但审核状态仍为待审核；首屏自动校验全部失败时「AI 校验」入口仍可见且可点击重试；AI 关闭态入口数为 0、核心预约可用且不发探测请求 |
| 15 | `web/e2e/specs/40-rules.spec.js` | 20 | 见第六节「核心业务规则用例点名」逐条列示 |
| — | **合计** | **98** | — |

辅助设施（非用例，`web/e2e/` 下）：`playwright.config.js`（串行/Chromium/超时/报告）、`global-setup.js`（后端探活并给出端口冲突修法）、`helpers/api.js`（HTTP 断言封装）、`helpers/auth.js`（登录态注入）、`helpers/admin.js`、`helpers/ui.js`、`helpers/ai.js`、`helpers/data.js`（教室/页面夹具）、`helpers/stable-seed.js`（稳定造数：先冲突检测挑空档再提交，规避单账号单会话 Token 互顶与共享种子数据冲突）。

## 四、覆盖范围（15 个 spec ↔ 13 个页面）

需求文档 1.3 / 2.4 定义 13 个页面（公共 2 + 学生端 5 + 管理端 6），E2E 逐页对应如下：

| 需求文档页面（13 页） | 对应 spec 文件 | 是否逐页覆盖 |
| --- | --- | --- |
| 1 登录页（公共） | `10-auth.spec.js`、`00-smoke.spec.js` | ✅ |
| 2 注册页（公共） | `10-auth.spec.js` | ✅ |
| 3 教室列表页（学生首页，含 AI 推荐卡/快速预约入口） | `11-classroom-list.spec.js`、`30-ai.spec.js`、`00-smoke.spec.js` | ✅ |
| 4 教室详情页 | `12-classroom-detail.spec.js`、`40-rules.spec.js` | ✅ |
| 5 我的预约页 | `13-my-reservation.spec.js` | ✅ |
| 6 个人中心页 | `15-profile.spec.js` | ✅ |
| 7 预约日历总览页（亮点） | `14-calendar.spec.js`、`40-rules.spec.js`（日历区间参数校验） | ✅ |
| 8 后台首页 | `20-admin-home.spec.js`、`00-smoke.spec.js` | ✅ |
| 9 教室资源管理页 | `21-classroom-manage.spec.js` | ✅ |
| 10 预约审核管理页（含 AI 校验标签、批量审核） | `22-audit-manage.spec.js`、`30-ai.spec.js`、`40-rules.spec.js` | ✅ |
| 11 用户管理页 | `23-user-manage.spec.js` | ✅ |
| 12 预约记录页（Excel 导出） | `24-reservation-record.spec.js` | ✅ |
| 13 数据看板页（亮点，ECharts 三图） | `25-dashboard.spec.js` | ✅ |

覆盖维度：

- **页面维度**：13 页全部有逐页或跨页 spec 覆盖（12 个页面级 spec + 00-smoke 骨架冒烟）。
- **跨页组件维度**：`30-ai.spec.js` 覆盖 AI 推荐卡、AI 快速预约、悬浮助手、审核页 AI 校验四处跨页 AI 交互及其关闭态退化。
- **业务流程维度**：注册/登录 → 教室浏览筛选 → 冲突检测 → 提交预约 → 我的预约/取消 → 管理端审核（单条/批量）→ 预约记录导出 → 数据看板/日历，全链路打通。
- **规则维度**：`40-rules.spec.js` 用接口直调方式复核核心业务规则与鉴权（见第六节）。
- **权限维度**：未登录 401、学生访问管理员接口 403、管理员访问学生接口放行、前端路由守卫（未登录回跳 / 角色不符回自己首页）。

## 五、通过情况

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `npm run lint`（ESLint，`--max-warnings 0`） | **0 error / 0 warning** | 通过；阶段3/4/5 三轮改动均未引入告警 |
| `npm run build`（Vite 5） | **成功，2101 modules transformed** | 通过 |
| E2E 全量（15 spec / 98 用例） | **98 passed / 98 total** | 全部通过；耗时 1.9m；1 worker；Chromium；重试 0 |
| 数据库基线复核 | **预约 13 / 用户 5 / 教室 12 / E2E 临时教室残留 0** | 通过；用例自建数据（E2E 前缀预约、自建教室、临时账号）已清理 |
| 13 页功能完整性 | 13/13 页面有覆盖 | 通过（见第四节） |
| 核心规则 | 冲突检测 / 状态流转 / 取消时限 / 越权 / 参数校验均有对应用例且全通过 | 通过（见第六节） |

失败用例：无。跳过用例：无（`test.skip` 在最终结果中计数为 0）。

## 六、核心业务规则用例点名

`40-rules.spec.js`（20 条，全部接口直调或页面级断言）逐组覆盖：

| 规则（需求文档 1.4） | 用例 | 结果 |
| --- | --- | --- |
| 冲突检测公式 `新开始 < 旧结束 AND 新结束 > 旧开始` | 「六组边界：仅与『已通过』预约按重叠公式判定」 | ✅ |
| 仅「已通过」参与冲突判定 | 「待审核 / 已取消的预约不构成冲突」 | ✅ |
| 后端二次校验（防绕过前端） | 「绕过前端直调提交接口，重叠时段仍被拒绝」 | ✅ |
| 前端实时校验 | 「重叠时段提示冲突且按钮禁用，改为不冲突时段后按钮可用」「开始时间不早于结束时间：前端提示且不发冲突检测请求」 | ✅ |
| 状态流转（待审核0 → 已通过1 / 已驳回2） | 「待审核 → 已通过：落库且不可重复审核」「待审核 → 已驳回：驳回必填备注、驳回后不可再审」 | ✅ |
| 状态流转（待审核0 / 已通过1 → 已取消3） | 「待审核 / 已通过 → 已取消；已取消不可再取消」「只能取消自己的预约（跨用户被拒）」 | ✅ |
| 审核前复查冲突（防「双已通过」） | 「同教室同时段先后通过，第二条被拒」 | ✅ |
| 批量审核约束（仅待审核参与） | 「批量审核：仅待审核参与，已通过不被改动，返回实际更新条数」「批量驳回必须填写审核备注」 | ✅ |
| 取消时限（开始前 1 小时内禁止） | 「开始前 1 小时内禁止取消，超过 1 小时可取消」 | ✅ |
| 参数校验 | 「分页参数：page<1 与 size>500 均返回 400」「日历区间：缺参 / 区间倒置 / 跨度过大均返回 400」 | ✅ |
| 越权 401 / 403 | 「未登录访问受保护接口 → HTTP 401」「学生 Token 访问管理员专属接口 → HTTP 403」「管理员 Token 访问学生端接口正常放行」 | ✅ |
| 前端路由守卫 | 「未登录访问受保护页面跳登录页并带回跳地址」「角色不符访问对方端页面回自己首页」 | ✅ |

其他 spec 的核心规则覆盖：

- **收藏上限 10**（需求文档 1.4「每人最多收藏 10 间」）：`12-classroom-detail.spec.js`「收藏上限 10：第 11 间被拒并提示，结束后还原原收藏集」。
- **预约规则口径下发**：`11-classroom-list.spec.js` / `12-classroom-detail.spec.js` 的实时校验依赖 `/api/config/booking-rules`（时段窗口与时长上限由后端下发，前端不再手工镜像）。
- **AI 只读不写**：`30-ai.spec.js`「AI 快速预约：解析结果回填表单 → 选教室 → 取消不写库」「管理端 AI 合规校验：显示校验结果，但审核状态仍为待审核」。
- **AI 关闭退化**（需求文档 3.4「一键启停」）：`30-ai.spec.js`「AI 关闭态：入口数为 0，核心预约功能可用且不发探测请求」。
- **分组冒烟/其余规则**：`20-admin-home.spec.js` 卡片数值与 `/api/stats/overview` 逐字段一致；`24-reservation-record.spec.js` 导出文件名为 xlsx。

## 七、遗留问题（如实记录，不影响验收）

1. **`.room-card` 未加 `role="button"` 属主动排除，非遗漏**：`ClassroomList.vue` 的教室卡片内部已有真实按钮，若外挂按钮语义会造成嵌套交互与双 Tab 停靠，故阶段5 主动排除。若后续调整该卡片内部结构（移除内部按钮），需重新评估是否补键盘语义。
2. **无障碍覆盖边界**：阶段5 仅修复「div 式可点击元素键盘不可达」这一类问题（`role="button"` + `tabindex="0"` + Enter/空格激活，共 6 个既有文件 9 类 19 处）；未做完整 WCAG 审计（对比度、读屏全流程脚本化验证等），属已知未覆盖范围。
3. **E2E 强依赖本地环境**：用例依赖本地容器 MySQL / Redis 与**已启动的后端**（Playwright 不自动起后端，仅在 `globalSetup` 探活并给出端口修法）；端口不符（如容器 MySQL 映射 3307 而后端默认 3306）会导致登录 500 → 探活失败。E2E 串行执行（1 worker），并发会污染共享库的时段与计数断言。
4. **AI 为可降级的外部依赖**：E2E 只在「关闭态」与「结构/入口/只读」层面断言 AI（不断言模型具体文案）；启用后无密钥/限流/超时走本地规则降级，模型输出本身不可复现，故未纳入回归断言。
5. **AI 真实链路受上游限流影响**：接入真实密钥后可能命中上游限流（429），此时自动降级为本地规则模式；该行为属设计内降级，不影响核心业务，但真实模型输出的稳定性不由本项目保证。
6. **未执行的检查项（如实声明）**：本轮未运行后端 `mvn test` 单元测试与历史轮次的 `docs/test-script.ps1` 接口级脚本，交付测试结论以「lint + build + 98 条 E2E + 数据库基线」为准（后端已处于 `v1.0-backend-freeze` 冻结状态，本轮未改动后端代码）。

> 结论：交付版本（HEAD `11862a9`）静态检查零告警、生产构建通过、98 条浏览器级端到端用例全通过、数据库回到交付基线；13 页与核心业务规则覆盖完整；遗留项均为已知边界与主动取舍，无未修复的核心缺陷。