# AGENTS.md — 项目协作章程（所有 Agent 的最高行动准则）

> 本文件为实验室/教室预约管理系统项目所有开发 Agent 的总章程。**唯一的开发依据是《需求设计文档.md》**。所有开发行为必须严格遵循本规范，禁止自行决策、越界开发。项目负责人仅在每轮交付时做最终验收。

---

## 1. 总则

1. **唯一需求依据**：《需求设计文档.md》（V3.1）为唯一开发基准。任何功能增减、规则修改、表结构调整必须经项目负责人书面确认，任何 Agent 无权自行决策。
2. **核心目标**：8 周周期内交付可直接答辩的优秀级项目，做到核心逻辑严谨、代码规范统一、亮点突出、零核心 Bug。
3. **协作原则**：角色边界清晰，上下游交付标准明确，问题逐级上报，不交叉、不越权、不自行兜底。
4. **技术栈边界**：严格使用《需求设计文档.md》2.2 节指定的技术栈与版本，禁止私自引入额外依赖、框架或中间件。
5. **环境基线**：JDK 21 LTS + IDEA 内置 Maven 3.9.x + Node.js 22.x + MySQL 8.0.x。后端统一 `jakarta.*` 包（Spring Boot 3），禁止出现 `javax.*`。

---

## 2. 角色分工与职责边界

| 角色 | 核心职责 | 输入 | 输出 | 禁止行为 |
|------|----------|------|------|----------|
| **项目架构师 Agent** | 总规范负责人；每轮任务拆解；前后端接口定义；技术方案裁决；代码规范仲裁；争议问题最终判定 | 迭代计划、需求文档、负责人下达的调整指令 | 《本轮任务说明书》《接口定义清单》《规范裁决结论》 | 私自新增需求、修改核心业务规则、突破技术栈边界 |
| **后端开发 Agent** | 后端全量代码；数据库实体/分层逻辑；业务逻辑；AI 模块封装；接口联调；单元自测 | 任务说明书、接口定义规范 | 可运行后端工程、接口清单、单元自测用例 | 私自修改表结构、引入未约定依赖、简化冲突检测/状态流转逻辑 |
| **前端开发 Agent** | 前端页面实现；公共组件封装；交互逻辑；接口对接；UI 还原；AI 交互组件开发 | 任务说明书、后端接口清单 | 可运行前端工程、页面交互说明 | 私自新增页面、修改核心交互流程、脱离接口规范自行定义字段 |
| **测试验证 Agent** | 功能全量测试；边界场景测试；异常操作测试；权限校验；Bug 回归验证；输出测试报告 | 待交付代码、任务说明书 | 《本轮测试报告》、问题清单、验证结论 | 隐瞒核心 Bug、简化测试用例、遗漏边界场景 |

---

## 3. 单轮标准迭代工作流（闭环）

每一轮严格执行 5 步流程，负责人仅在第 5 步介入。

```
任务下达 → 架构拆解 → 并行开发 → 测试验证 → 交付验收
```

### 第 1 步：任务下达
架构师根据总迭代计划输出《本轮任务说明书》，明确：任务范围、需求依据（引用需求文档章节）、验收标准、交付时间。所有任务必须可追溯到《需求设计文档.md》，禁止无依据开发。

### 第 2 步：架构拆解
架构师完成模块拆分，定义前后端接口路径、参数、返回格式，输出《接口定义清单》，作为前后端开发的统一依据。

### 第 3 步：并行开发
1. 后端优先完成接口开发，输出接口清单给前端。
2. 前端先完成静态页面与交互，接口就绪后完成联调。
3. 开发中遇到需求歧义，提交架构师裁决，**禁止自行理解修改**。

### 第 4 步：测试验证
1. 测试 Agent 基于任务说明书做全量验证：正常流程、边界场景、异常操作、权限校验。
2. 发现 Bug 直接返回对应开发 Agent 修复，回归测试通过后方可提交交付。
3. 输出《本轮测试报告》：测试用例、通过情况、覆盖范围、遗留问题。

### 第 5 步：交付验收
按统一格式打包交付，提交负责人验收：
- 验收通过 → 进入下一轮迭代
- 验收不通过 → 明确问题点，返回测试 Agent 组织修复，回归通过后重新提交

---

## 4. 强制开发规范

### 4.1 需求边界规范
- 严格按照需求文档开发，禁止自行新增功能、扩展场景、调整数据库表结构。
- 核心业务规则必须 100% 实现，禁止简化：
  - 预约冲突检测公式：`新开始 < 旧结束 AND 新结束 > 旧开始`
  - 预约状态流转：待审核(0) → 已通过(1)/已驳回(2)；待审核/已通过 → 已取消(3)
  - 前后端双重校验机制：前端实时校验 + 后端二次校验

### 4.2 技术栈规范（禁止私自增减）
| 层级 | 技术与版本 |
|------|------------|
| JDK | 21 LTS |
| 构建工具 | IDEA 内置 Maven 3.9.x |
| 核心框架 | Spring Boot 3.2.10 LTS |
| ORM | MyBatis-Plus 3.5.7（mybatis-plus-spring-boot3-starter） |
| 接口文档 | Knife4j 4.4.0（openapi3-jakarta） |
| Excel | EasyExcel 3.4.0 |
| 工具库 | Hutool 5.8.32 + Lombok 1.18.34 |
| 数据库 | MySQL 8.0.x |
| 前端运行环境 | Node.js 22.x |
| 前端框架 | Vue 3.4 + Vite 5 |
| UI 组件 | Element Plus 2.7.x |
| 状态/路由 | Pinia 2.x + Vue Router 4.x |
| 亮点插件 | ECharts 5.5 + FullCalendar 6.1 + day.js 1.11 |
| AI 服务 | Agnes AI（agnes-2.0-flash，国内节点 https://api.agnes-ai.cn/v1） |

### 4.3 代码架构规范
1. **后端分层**：严格 `controller → service → mapper → entity` 四层结构，禁止跨层调用，业务逻辑禁止写在 controller 层。
2. **统一返回**：所有接口返回 `Result`：`{ "code": 200, "message": "操作成功", "data": {} }`；错误码语义化。
3. **全局异常**：必须实现全局异常处理器，禁止将异常堆栈直接返回前端。
4. **包名规范**：统一 `jakarta.*`，禁止 `javax.*`。
5. **命名规范**：
   - 后端：类名大驼峰，方法/变量小驼峰，数据库表/字段下划线命名
   - 前端：组件名大驼峰，文件短横线命名，方法/变量小驼峰
6. **注释要求**：类、接口、核心方法必须加功能注释；冲突检测、状态流转等核心逻辑必须加行内注释并标注规则依据。
7. **禁止硬编码**：魔法值定义为常量类；密钥仅存配置/环境变量。
8. **前端规范**：页面、组件、路由、接口封装目录分离，禁止单文件写全部逻辑；Axios 统一封装（携带 Token、统一错误提示）。

### 4.4 AI 模块专属约束
1. 密钥仅配置在 `application.yml` 或环境变量，禁止硬编码，禁止前端直接调用。
2. 所有 AI 代码独立存放于 `ai` 包，低耦合调用业务数据，不侵入原有业务逻辑。
3. 调用超时 60 秒；超时/报错/限流时自动降级为本地规则模拟模式，不阻断业务。
4. Prompt 严格限定场景与结构化输出格式，无关问题统一返回预设话术。
5. AI 只读不写：所有业务操作必须用户手动确认执行。
6. 全局配置项 `ai.enable` 一键启停所有 AI 功能，关闭后不影响核心系统运行。
7. 所有 AI 生成内容，前端标注「AI 生成，仅供参考」。

---

## 5. 交付物标准

每轮交付必须按以下目录结构打包，确保负责人拿到即可运行、即可验收。

```
第X轮交付-模块名称
├── 代码/
│   ├── reservation-server/  # 后端完整可运行工程
│   └── reservation-web/     # 前端完整可运行工程
├── 文档/
│   ├── 本轮任务说明.md      # 任务范围、需求依据（引用需求文档章节）
│   ├── 接口清单.md          # 新增接口：路径、参数、返回值
│   └── 测试报告.md          # 测试用例、通过情况、覆盖范围、遗留问题
└── 启动说明.md              # 环境要求、启动步骤、验证方法
```

---

## 6. 验收机制

### 6.1 验收三步法
1. **文档核验**：先看测试报告与接口清单，确认本轮功能点全覆盖、基础测试通过。
2. **规范核验**：对照本规范与需求文档，检查分层结构、命名规范、统一返回格式、`jakarta.*` 包名正确性。
3. **运行验证**：启动项目，实际走通核心流程，验证关键逻辑（含边界场景）。

### 6.2 结果处理
- **通过**：确认进入下一轮迭代。
- **退回修改**：明确列出问题点，返回测试 Agent 组织修复，回归通过后重新提交。

### 6.3 审阅优先级原则
核心业务逻辑 > 代码规范 > 功能完整 > UI 细节。优先保证冲突检测、状态流转、权限控制正确。

---

## 7. 红线（绝对禁止）

1. 私自新增/删除功能、修改业务规则、调整表结构。
2. 私自引入技术栈之外的依赖或中间件（Redis 等加分项须先经负责人确认）。
3. 简化核心冲突检测、状态流转、权限控制逻辑。
4. 将 API 密钥、密码明文硬编码进代码。
5. 跳过测试验证直接提交交付。
6. 私自降低交付标准（交付骨架、教程、占位代码冒充完成品）。
