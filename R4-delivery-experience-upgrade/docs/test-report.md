# R4 轮次测试报告 —— 体验升级（个人中心 / 收藏全链路 / 体验细节）

## 一、测试环境与前置

| 项 | 值 |
| --- | --- |
| 操作系统 | Windows（PowerShell 5.1） |
| 后端 | Spring Boot 3.2.10 / JDK 21 / MyBatis-Plus 3.5.7，运行于 http://localhost:8080 |
| 前端 | Vue 3.4.38 / Vite 5.4.21 / Element Plus 2.7.8，运行于 http://localhost:5173 |
| 数据库 | Docker MySQL 8.0（reservation-mysql，库 reservation，root/root，utf8mb4） |
| 基线 | R1 21 + R2 50 + R3 54 = 125 条既有用例（T01-T127）+ R4 新增 28 条（T128-T155）= **153 条** |
| 测试脚本 | `docs/test-script.ps1`（复用 R3 脚本并追加 R4 段，可重复执行，结束后恢复初始基线） |

## 二、总结果

```
用例总数: 153   通过: 153   失败: 0
```

- 覆盖分类：正常流程 62 条 / 边界场景 43 条 / 异常操作 26 条 / 权限校验 22 条（R1-R4 合计）。
- R4 新增 28 条分类：正常流程 13 / 边界场景 7 / 异常操作 3 / 权限校验 5。

## 三、R4 新增用例明细（T128-T155，共 28 条，全部通过）

### 1. 个人中心-数据统计（GET /api/user/stats）

| 编号 | 用例名称 | 类别 | 结果 |
| --- | --- | --- | --- |
| T128 | 新用户数据统计全零（无预约基线） | 正常流程 | 通过 |
| T129 | 创建 3 条预约并审核（2 通过 1 驳回，数据准备） | 正常流程 | 通过 |
| T130 | 统计：累计 3 / 本月动态 / 通过率 66.7 / 最近一次非空 | 正常流程 | 通过 |
| T131 | 管理员访问数据统计（200） | 权限校验 | 通过 |
| T132 | 未登录访问数据统计（401） | 权限校验 | 通过 |

### 2. 个人信息（PUT /api/user/info）

| 编号 | 用例名称 | 类别 | 结果 |
| --- | --- | --- | --- |
| T133 | 修改个人信息生效（姓名/邮箱/手机号；回读不含 password、账号不变） | 正常流程 | 通过 |
| T134 | 姓名为空被拒（400） | 边界场景 | 通过 |
| T135 | 手机号格式错误被拒（400） | 边界场景 | 通过 |
| T136 | 姓名超长（>20 字符）被拒（400） | 边界场景 | 通过 |
| T137 | 未登录修改个人信息（401） | 权限校验 | 通过 |

### 3. 修改密码（PUT /api/user/password）

| 编号 | 用例名称 | 类别 | 结果 |
| --- | --- | --- | --- |
| T138 | 原密码错误被拒（400） | 异常操作 | 通过 |
| T139 | 新密码少于 6 位被拒（400） | 边界场景 | 通过 |
| T140 | 两次新密码不一致被拒（400） | 边界场景 | 通过 |
| T141 | 缺少密码参数被拒（400） | 异常操作 | 通过 |
| T142 | 改密后新密码可登录、旧密码失效（BCrypt 已更新） | 正常流程 | 通过 |
| T143 | 未登录修改密码（401） | 权限校验 | 通过 |

### 4. 收藏全链路（POST /api/favorite/{id} / GET /api/favorite/list）

| 编号 | 用例名称 | 类别 | 结果 |
| --- | --- | --- | --- |
| T144 | 收藏教室（新增成功，data=true） | 正常流程 | 通过 |
| T145 | 重复收藏自动取消（toggle，data=false） | 正常流程 | 通过 |
| T146 | 收藏列表返回该条（含教室名称/楼栋/编号等字段） | 正常流程 | 通过 |
| T147 | 收藏不存在的教室被拒（400「教室不存在」） | 异常操作 | 通过 |
| T148 | 收藏第 11 间被拒（400「收藏数量已达上限（10 间）」） | 边界场景 | 通过 |
| T149 | 收藏列表上限 10 条 | 正常流程 | 通过 |
| T150 | zhangsan 基线收藏 2 条（数据基线回归） | 正常流程 | 通过 |
| T151 | 管理员访问收藏列表（200） | 权限校验 | 通过 |
| T152 | 未登录访问收藏列表（401） | 权限校验 | 通过 |
| T153 | 未登录收藏教室（401） | 权限校验 | 通过 |

### 5. 体验细节（状态标签 / 登录提醒数据支撑）

| 编号 | 用例名称 | 类别 | 结果 |
| --- | --- | --- | --- |
| T154 | 状态标签三态有效（所有教室 ∈ {当前空闲/使用中/已结束}）+ 教室 12 按当前时刻推导口径正确（本次运行时为「已结束」） | 边界场景 | 通过 |
| T155 | 即将开始（24h 内）已通过预约可查（登录提醒数据支撑：start ∈ (now, now+24h)） | 正常流程 | 通过 |

## 四、前端 UI 验证（浏览器实测，Chrome 经 Browser Use 逐项核验）

| 体验点 | 验证方式 | 结果 |
| --- | --- | --- |
| 个人中心页面（/student/profile） | 导航进入 + 截图 OCR | 通过：数据概览 4/4/66.7%/2026-09-11 10:43 全部正确 |
| 常用教室快捷入口 | 页面渲染收藏卡片 | 通过：A201 物理实验室、A101 多媒体教室 2 张卡片，点击可直达详情 |
| 消息通知动态生成 | 页面渲染 + 未读计数 | 通过：5 条未读，覆盖 待审核/已通过/即将开始/已驳回 四类，含「全部标为已读」 |
| 我的预约今日置顶 | 页面渲染 | 通过：今日预约排首行并带「今日」标签 |
| 教室详情收藏按钮 | 页面渲染（A101，zhangsan 已收藏） | 通过：按钮显示「已收藏」（星标实心态） |
| 登录提醒 | 退出后重新登录 | 通过：弹出「预约即将开始提醒」——[B101化学实验室] 预约将于 2026-09-12 09:00 开始 |
| 状态标签三态 | 列表页标签渲染 | 通过：A101「当前空闲」、A201「已结束」等三态标签颜色区分 |
| 筛选条件记忆 | 搜索 A101 → 切页返回 | 通过：关键词还原为 A101，结果仍为 1 条 |
| 预约草稿自动保存 | 弹窗填用途 → 关闭 → 重开 | 通过：用途「R4草稿自动保存验证」自动回填 |
| 友好空状态 | 搜索不存在关键词 | 通过：「没有找到符合条件的教室，换个条件试试吧」+ 重置筛选按钮 |
| 前后端接口联通 | 浏览器 network 面板 | 通过：/api/user/info、/api/user/stats、/api/favorite/list、/api/reservation/mine 均 200 |

> 收藏 toggle 交互、个人信息保存、修改密码提交在 API 层已验证（T133-T149）；浏览器实测以只读验证为主，
> 避免污染基线数据（zhangsan 收藏记录、预约状态保持初始基线不变）。

## 五、数据基线恢复验证（脚本清理后）

```
sys_user: 5 人      classroom: 12 间
reservation: 13 条（待审核 3 / 已通过 6 / 已驳回 2 / 已取消 2）
user_favorite: 6 条
```

测试脚本可复跑：第二次运行（含 T128-T155 全量）同样 153/153 通过，且结束后数据库恢复至上述基线。

## 六、回归结论

- R1 21 + R2 50 + R3 54 = 125 条基线用例**零回归失败**（本次运行 T01-T127 全部通过）。
- R4 新增 28 条覆盖正常/边界/异常/权限四类，全部通过。
- 前端 11 项体验点浏览器实测全部生效。
- 交付代码可直接运行验证（后端 8080、前端 5173 均在本次测试中实际启动运行）。
