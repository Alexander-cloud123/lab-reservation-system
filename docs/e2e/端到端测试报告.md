# 端到端测试报告（E2E）

> 测试依据：`docs/e2e/端到端测试提示词.md`（四层：API 全量回归 / 浏览器端到端旅程 / 核心规则专项 / 安全回归）
> 实测日期：2026-09-12（今天）、2026-09-13（明天）、2026-09-14（后天）
> 环境：Docker `reservation-mysql`（MySQL 8.0，root/root，库 reservation）；后端 8080（IDEA Maven spring-boot:run）；前端 5173（Vite dev）
> 测试口径：交付配置 = `application.yml ai.enable=false` + `ai_config.ai_enable='false'`；联调模式 = 双开关均 true（无真实密钥，全程规则模拟降级）
> 测试账号：admin/admin123（role=1）；zhangsan/lisi/wangwu/zhaoliu 密码 123456（role=0）

## 一、总体结论

**端到端测试全部通过，未发现 P0/P1 级产品缺陷。**

| 层级 | 范围 | 结果 |
|---|---|---|
| API 全量回归（交付配置） | 228 用例 | **228/228 通过**（`run-final.log`） |
| API 全量回归（联调模式） | 234 用例 | **234/234 通过**（`run-integration.log`） |
| 浏览器 E2E · 学生旅程 | 9 步 | 全部通过（截图存证） |
| 浏览器 E2E · 管理员旅程 | 7 步 | 全部通过（截图存证） |
| 浏览器 E2E · AI 交互 | 推荐/快速预约/合规/悬浮助手 | 全部通过（截图存证） |
| 核心规则专项（H1/M2/H2） | 3 项 | **3/3 通过**（`run-special.log`） |
| 安全回归 | 401/403/越权/非法入参/AI 降级 | 全过（含 234 用例与专项） |

测试过程中无业务代码改动；仅生成测试脚本日期适配副本与临时开关切换，测试后已恢复交付配置与基线。

## 二、基线快照（恢复后验证）

```
sys_user=5（admin + 4 学生）     classroom=12
reservation=13（待审核 0:3 / 已通过 1:6 / 已驳回 2:2 / 已取消 3:2）
user_favorite=6     ai_config=5
ai_config.ai_enable='false'     application.yml ai.enable=false
id=13 purpose HEX=E5B08FE7BB84E5AE9EE9AA8CE58786E5A487（小组实验准备，UTF-8 正确、无双重编码）
```

## 三、API 全量回归

### 1. 交付配置：228/228 全过

- 脚本：`docs/iterations/R7-delivery-ai-integration/docs/test-script.ps1`（97KB）的日期适配副本 `.devtmp\gen\test-script-e2e.ps1`（硬编码日期整体 +1 天映射：2026-09-11→09-12、2026-09-05→09-06 等，单趟正则替换）。
- 首轮 226/228（`run-delivery.log`）：T119/T120 失败根因是**审查 P0 修复 M1（禁止过去日期预约）后脚本自身用例漂移**——T112 用 `$yesterday` 提交预约被 400 拒绝，预约 B 未创建，连锁使 T119（mine total 10→9）与 T120（待审核 2→1）计数不符。**非产品缺陷**。
- 修正：T112 改为「35 分钟后开始」动态时段（分钟对齐 5 的倍数，跨天则取明天 00:00-00:30），语义等价（仍处开始前 1 小时内、取消必被拒），不改计数预期。重跑 **228/228 全过**（`run-delivery2.log`），恢复基线后复跑 **228/228 全过**（`run-final.log`）。
- 覆盖：认证/权限/统一返回 21、预约核心 54、看板统计 33、记录/导出/日历 34、AI 关闭态降级等。

### 2. 联调模式：234/234 全过

- 前置：`application.yml ai.enable=true`（注释「E2E 联调模式临时开启」）+ `ai_config.ai_enable='true'`，重启后端（`backend-start.log`）。
- 结果：**234/234 全过**（`run-integration.log`），比交付配置多 6 条 AI 启用态链路用例（推荐/解析/问答/合规 × 启用与降级）。
- 全程未配置真实 AGNES_API_KEY → 自动规则模拟降级（T237 无密钥降级、T238 限流降级均实测通过）。

## 四、浏览器端到端用户旅程（computer_use 浏览器，全程截图）

### 1. 学生端（lisi/zhangsan）— 9 步全部通过

| 步骤 | 实测结果 |
|---|---|
| 1 登录 | lisi/123456 登录成功；张三登录出现「预约即将开始提醒」（B101 09-13 09:00 即将开始）✓ |
| 2 教室列表 + AI 推荐 | 12 间；AI 推荐 Top3（lisi：C101/C401/C301；zhangsan：B102/B201/A201，个性化生效）+「AI 生成，仅供参考」✓ |
| 3 楼栋/关键词筛选 | 信息楼→4 间（A101/A102/A201/A301）；关键词 B102→详情页 id=6 ✓ |
| 4 预约提交 | B102 明天 09:30-11:30「生物实验预习」→「预约提交成功，待管理员审核」✓ |
| 5 冲突检测（前端实时校验） | A201 今天 15:30-16:30 → 红色提示「该时段与『15:00-17:00』的已通过预约冲突」+ **提交按钮禁用** ✓ |
| 6 我的预约/取消 | 4 条记录、今日置顶（A201 标「今日」）；取消 B102 → 二次确认弹窗「取消后不可恢复」→ 确认后待审核→已取消「取消成功」✓ |
| 7 收藏 | 浏览器按钮点击无响应（bu 工具交互受限，见缺陷清单）；API 互补验证 POST /api/favorite/3「收藏成功」+ 列表新增 A201（id=40），个人中心常用教室含 A201 ✓ |
| 8 个人中心 | 数据概览（累计 4/本月 4/通过率 100%）、常用教室 4 间、消息通知 2 条未读 ✓ |
| 9 日历总览 | 月视图 2026-09 各日色块与库一致（12 日 5 条多状态、13 日 3 条）、状态图例/月周切换/今天按钮 ✓ |

### 2. 管理员端（admin）— 7 步全部通过

| 步骤 | 实测结果 |
|---|---|
| 1 登录 | admin/admin123 登录成功（radio 需 JS 触发 change）→ 后台首页（欢迎语+账号信息卡）✓ |
| 2 预约审核 | 待审核 3 条均带「AI 校验：通过」标签；单条「通过」C101（id=7）→ 二次确认「确定通过」→「审核通过」✓ |
| 3 用户管理 | 5 用户列表、角色/状态筛选、禁用/重置密码操作列 ✓ |
| 4 教室管理 | 12 间分页、编辑/停用/删除操作列、新增教室入口 ✓ |
| 5 预约记录 | 14 条记录、日期/教室/用户/状态筛选、导出 Excel 入口 ✓ |
| 6 数据看板 | 教室使用率排行/热门时段分布/月度预约趋势三图渲染正常，近 7/30/90 天切换 ✓ |
| 7 权限守卫 | 路由直跳 /admin/audit 被守卫拦回（正确路径 /admin/audits），菜单导航正常 ✓ |

### 3. AI 交互（联调模式，zhangsan）— 全部通过

| 场景 | 实测结果 |
|---|---|
| AI 快速预约 | 「明天下午2点到4点 40人 机房 做课程设计」→ 智能解析 → 参数预填（09-13 14:00-16:00、40 人、课程设计）→ 匹配机房 A301/B201/C301 → 选 B201 →「该时段可预约」→ 提交成功「预约提交成功，等待管理员审核」✓ |
| AI 悬浮助手 | 「怎么取消预约？」→ FAQ 命中「待审核或已通过的预约可在预约开始前 1 小时自由取消（…需二次确认）；开始前不足 1 小时不可取消…」；底部「AI 生成，仅供参考 · 仅解答预约相关问题」✓ |
| AI 合规校验 | 审核页待审核记录显示「AI 校验：通过」标签（T235/T236 API 层违规词/正常用途判定均过）✓ |

## 五、核心规则专项（提示词第 3 层，`run-special.log`）

| # | 专项 | 构造 | 实测 | 结论 |
|---|---|---|---|---|
| H1 | 过期 Token 拒绝（审查 P0） | 手工 HS256 签名 exp 在过去 1 小时的 Token（默认密钥） | 访问 /api/user/info → **HTTP 401**；有效 Token 控制组 200 | **通过** |
| M2 | 并发竞态（审查 P0 行锁） | lisi/wangwu 同时提交 C201 09-14 10:00-12:00（Start-Job 并行） | 两条均创建成功（待审核 id=59/60）——**符合设计**：冲突检测仅针对已通过预约，待审核不占用时段；由 H2 审核复查兜底 | **通过**（M2+H2 组合防线） |
| H2 | 审核通过复查冲突（审查 P0） | 审核 59 通过后，审核 60 同教室同时段 | 审核 59 成功 → 审核 60 → **400「审核失败：与已通过预约『10:00-12:00』时间冲突，不能通过」** | **通过** |

> 说明：专项首轮 H2a 曾用 09-13 提交与 id=1（09-12）对比，日期不同不冲突属构造错误；修正为同日期后复查拦截成立。**非产品缺陷**。

## 六、安全回归（提示词第 4 层）

| 类别 | 用例（234 全量 + 专项） | 结果 |
|---|---|---|
| 未登录访问 | T17/T40/T71/T82/T90/T102/T125/T126/T127/T132/T137/T207/T213-T216 等 | 全过（401） |
| 伪造/篡改 Token | T18 伪造、T21 篡改签名、T219 AI 伪造 Token | 全过（401） |
| 过期 Token | 专项 H1 | 通过（401） |
| 角色越权 | T19/T37/T38/T39/T65-T70/T122/T123/T124/T206（学生访问管理接口 403）、T20（管理员访问普通接口 200） | 全过（403） |
| 数据级越权 | T111 取消他人预约被拒「只能取消自己的预约」 | 通过 |
| 非法入参 | T06-T16 注册登录、T35/T36/T54/T55/T58/T63/T81/T88/T89/T97-T100/T105/T107/T115/T121/T134-T136/T138/T139/T204/T205/T226-T229 | 全过（400 语义化） |
| AI 降级 | T237 无密钥降级、T238 限流降级、T239 响应≤3s；关闭态 enabled=false 友好提示（不 500，交付配置复测） | 全过 |
| 数据安全 | T208/T211 不含 password | 全过 |

## 七、缺陷清单

**未发现 P0/P1 产品缺陷**。记录如下：

| # | 类型 | 说明 | 处置 |
|---|---|---|---|
| 1 | 工具限制（非产品缺陷） | bu 浏览器会话对 Element Plus 表格行内按钮（审核「通过」）、详情页「收藏」按钮的点击事件触发不稳定（JS click 后偶发不生效/无响应）；el-table 复选框勾选不可用（R8 同款限制） | 已用 API 接口直调互补验证（收藏/审核/批量审核），业务功能确认正常；**按钮交互与批量勾选留真机人工复核** |
| 2 | 脚本问题（非产品缺陷） | 首轮 T119/T120 计数失败：T112 用「昨天」日期提交被 M1 修复（禁止过去日期）400 拒绝，连锁计数漂移 | 脚本适配为「35 分钟后开始」，复跑 228/228 全过 |
| 3 | 观察记录 | 前端「收藏」按钮 bu.click 无响应但 API 正常；同场景「审核通过」需 JS 确认弹窗后生效——疑似 Element Plus 按钮事件在自动化会话中的时序问题，未在真机复现 | 列入真机复核清单 |

## 八、测试后恢复确认（2026-09-12）

```
1. application.yml：ai.enable=true → false（注释已移除「E2E 联调模式临时开启」）
2. ai_config：config_value 'true' → 'false'（WHERE config_key='ai_enable'）
3. 后端重启为交付配置（backend-delivery-start.log），复测 AI 接口：code=200 enabled=false「AI 服务未启用，当前为纯预约系统模式」不 500
4. 重导基线：docker cp init_db.sql → 容器内 mysql --default-character-set=utf8mb4（避免双重编码）
   → sys_user=5 / classroom=12 / reservation=13（0:3/1:6/2:2/3:2）/ user_favorite=6 / ai_config.ai_enable=false
5. 交付配置 228/228 复跑全过（run-final.log），脚本自清理段确认 id>13 预约与测试收藏为 0
```

## 九、覆盖缺口与真机复核清单

1. **批量审核 UI 勾选**：bu 会话无法勾选 el-table 复选框，改用接口直调验证（T117/T118 批量通过/混合全过）；真机浏览器现场演示。
2. **「收藏」按钮与表格行内按钮点击**：自动化会话偶发不触发，API 层已验证；真机复核按钮提示与状态切换。
3. **导出 Excel**：T209 已验证合法 xlsx 文件头与 5119 字节；真机下载后用 Excel/WPS 打开复核。
4. **删除/禁用等破坏性操作**：API 层全过（T48-T64/T27-T34）；真机按提示词「执行前确认」演示。
5. **加载性能/弱网**：未纳入本轮（T239 AI 响应 ≤3s 已覆盖接口级）；如有答辩需要可在真机 DevTools 观察。

## 十、结论

- 交付配置 **228/228**、联调模式 **234/234**、专项 **3/3**、浏览器 E2E 学生 9 步 + 管理员 7 步 + AI 交互全通过——与 R1-R8 基线一致、零回归。
- 审查 P0 修复（H1 过期 Token、H2 审核复查冲突、M1 禁止过去日期）均实测生效；M2 并发行锁与 H2 复查构成最终一致性防线。
- 数据库已恢复初始基线、双开关已复位，系统处于交付配置（ai.enable=false），可直接进入验收/答辩。
