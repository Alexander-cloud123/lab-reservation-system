# R5 轮次测试报告 —— 亮点功能（日历总览 / 数据看板）

## 一、测试环境与前置

| 项 | 值 |
| --- | --- |
| 操作系统 | Windows（PowerShell 5.1） |
| 后端 | Spring Boot 3.2.10 / JDK 21 / MyBatis-Plus 3.5.7，运行于 http://localhost:8080 |
| 前端 | Vue 3.4.38 / Vite 5.4.21 / Element Plus 2.7.8 / ECharts 5.5.1 / FullCalendar 6.1.15，运行于 http://localhost:5173 |
| 数据库 | Docker MySQL 8.0（reservation-mysql，库 reservation，root/root，utf8mb4） |
| 基线 | R1 21 + R2 50 + R3 54 + R4 28 = 153 条既有用例（T01-T155）+ R5 新增 27 条（T156-T182）= **180 条** |
| 测试脚本 | `docs/test-script.ps1`（复用 R4 脚本并追加 R5 段，可重复执行，结束后恢复初始基线） |

## 二、总结果

```
用例总数: 180   通过: 180   失败: 0
```

- 覆盖分类（R1-R5 合计）：正常流程 72 / 边界场景 49 / 异常操作 33 / 权限校验 29（其中库一致性 3 条归入正常流程/边界场景）。
- R5 新增 27 条分类：正常流程 10 / 边界场景 6 / 异常操作 7 / 权限校验 7（含库一致性 SQL 对照 3 条）。
- 脚本可复跑验证：首轮运行后修复 3 条断言（教室 ID 映射 / 时段桶标签格式），**第二轮全量重跑 180/180 通过**，结束后数据库恢复初始基线。

## 三、R5 新增用例明细（T156-T182，共 27 条，全部通过）

### 1. 日历区间查询（GET /api/reservation/calendar）

| 编号 | 用例名称 | 类别 | 结果 |
| --- | --- | --- | --- |
| T156 | 日历区间查询（2026-09-05~09-12 返回基线 13 条，含教室展示字段、不含 password） | 正常流程 | 通过 |
| T157 | 按教室筛选（A101=2 条待审核） | 正常流程 | 通过 |
| T158 | 缺 startDate（400「缺少必要参数：startDate」） | 异常操作 | 通过 |
| T159 | 缺 endDate（400「缺少必要参数：endDate」） | 异常操作 | 通过 |
| T160 | 日期倒序（400「开始日期不能晚于结束日期」） | 异常操作 | 通过 |
| T161 | 跨度 367 天（400「日期区间跨度不能超过 366 天」） | 异常操作 | 通过 |
| T162 | 跨度 366 天（最大合法边界，200） | 边界场景 | 通过 |
| T163 | 学生访问日历区间（登录即可，200） | 权限校验 | 通过 |
| T164 | 未登录访问日历区间（401） | 权限校验 | 通过 |
| T165 | 单日区间（2026-09-11 = 5 条） | 正常流程 | 通过 |

### 2. 教室使用率排行（GET /api/stats/usage-rate）

| 编号 | 用例名称 | 类别 | 结果 |
| --- | --- | --- | --- |
| T166 | 默认近 30 天：12 间全量排行，top=A201 使用率 0.5（区间内已通过 5 条 ×2h ÷ 30天×14h） | 正常流程 | 通过 |
| T167 | 指定区间 09-05~09-12：B101 使用率 1.8（2h ÷ 8天×14h） | 正常流程 | 通过 |
| T168 | **库一致性**：SQL 计算 B101 占用小时=2.0 → 期望 1.8，与 API 一致 | 库一致性 | 通过 |

### 3. 月度预约趋势（GET /api/stats/trend）

| 编号 | 用例名称 | 类别 | 结果 |
| --- | --- | --- | --- |
| T169 | 默认近 30 天：2026-09 共 11 条（基线 9/12 两条不在区间） | 正常流程 | 通过 |
| T170 | 指定区间 09-01~09-30：13 条 | 正常流程 | 通过 |
| T171 | **库一致性**：SQL 按月 COUNT=13，与 API 一致 | 库一致性 | 通过 |

### 4. 热门时段分布（GET /api/stats/time-distribution）

| 编号 | 用例名称 | 类别 | 结果 |
| --- | --- | --- | --- |
| T172 | 默认近 30 天六桶：08:00-10:00=2(40%) / 10:00-12:00=1(20%) / 14:00-16:00=2(40%) / 其余 0 | 正常流程 | 通过 |
| T173 | **库一致性**：指定区间 09-05~09-12 已通过 6 条，08:00-10:00 桶 SQL=3/6 → 50%，与 API 一致 | 库一致性 | 通过 |

### 5. 统计接口边界与权限

| 编号 | 用例名称 | 类别 | 结果 |
| --- | --- | --- | --- |
| T174 | 非法日期参数（400「日期格式不正确，应为 yyyy-MM-dd」） | 异常操作 | 通过 |
| T175 | 统计跨度超 366 天（400「统计区间跨度不能超过 366 天」） | 异常操作 | 通过 |
| T176 | 学生访问使用率排行（403） | 权限校验 | 通过 |
| T177 | 学生访问月度趋势（403） | 权限校验 | 通过 |
| T178 | 学生访问时段分布（403） | 权限校验 | 通过 |
| T179 | 未登录访问使用率排行（401） | 权限校验 | 通过 |
| T180 | 未登录访问月度趋势（401） | 权限校验 | 通过 |
| T181 | 未登录访问时段分布（401） | 权限校验 | 通过 |
| T182 | 统计接口不返回 password（数据安全） | 数据安全 | 通过 |

## 四、前端 UI 验证（浏览器实测，Chrome 经 Browser Use 逐项核验）

### 日历总览页（/student/calendar，学生账号 zhangsan）

| 验证点 | 验证方式 | 结果 |
| --- | --- | --- |
| 月视图渲染 | 截图 OCR 逐格核对 | 通过：2026 年 9 月 13 条预约全部落在正确日期（9/5 B301、9/6 C301、9/7 C201、9/8 A301、9/9 B102、9/10 B201、9/11 五条、9/12 两条） |
| 四状态色块 | 月视图色块分布 | 通过：待审核（A101×2/C101）、已通过（B301/A301/B102/C401/A201/B101）、已驳回（C201/A102）、已取消（C301/B201）位置与库一致 |
| 周视图切换 | 切换周视图（9/7-9/13） | 通过：预约分布正确 |
| 点击色块查看详情 | 点击 A101 14:00-16:00 待审核色块 | 通过：详情弹窗显示 教室/日期/时段/状态/用途 全部正确 |
| 点击空白日期快速预约 | 坐标点击格子空白处触发 dateClick | 通过：弹窗打开且日期预填为所点日期（9/8、9/12 验证） |
| 教室下拉 | 弹窗内选择教室 | 通过：12 间教室可选，A301/B101 选中生效 |
| 快速预约冲突检测（不冲突） | A301 + 9/8 + 08:00-10:00 | 通过：绿色提示「该时段可预约」 |
| 快速预约冲突检测（冲突） | B101 + 9/12 + 08:00-10:00（与已通过 09:00-11:00 重叠） | 通过：红色提示「该时段与「09:00-11:00」的已通过预约冲突」+ 提交按钮 disabled |

### 数据看板页（/admin/dashboard，管理员账号 admin）

| 验证点 | 验证方式 | 结果 |
| --- | --- | --- |
| 看板入口 | 管理端左侧菜单「数据看板」 | 通过：导航可达 |
| 三图渲染 | 截图 OCR | 通过：教室使用率排行柱状图（12 教室）、月度预约趋势折线图（2026-09=11）、热门时段分布环形图（六桶）全部渲染 |
| 时间筛选（近 7 天） | 点击近 7 天 | 通过：日期范围联动更新为 2026-09-05~09-11，三图数据同步重算（使用率升至 2%/1.5% 等，随区间缩短升高，口径正确） |
| 默认近 30 天 | 页面初始状态 | 通过：日期范围 2026-08-13~09-11，趋势 2026-09=11 与接口一致 |
| 图表数据与库一致 | API 返回 vs 数据库 SQL 对照（T168/T171/T173） | 通过：三图数据源全部与库一致 |

> 快速预约**提交**链路（前端校验 → POST /api/reservation → 后端二次校验）由 R3 用例 T95-T102 已覆盖 + R5 冲突检测 UI 实测补充；
> 浏览器实测以只读验证为主，避免污染基线数据（未实际提交预约）。

## 五、数据基线恢复验证（脚本清理后）

```
sys_user: 5 人      classroom: 12 间
reservation: 13 条（待审核 3 / 已通过 6 / 已驳回 2 / 已取消 2）
user_favorite: 6 条
```

测试脚本可复跑：**两轮完整运行**（首轮 177/180 定位 3 条断言修正、第二轮 180/180）均自动恢复至上述基线。

## 六、回归结论

- R1 21 + R2 50 + R3 54 + R4 28 = 153 条基线用例**零回归失败**（本次运行 T01-T155 全部通过）。
- R5 新增 27 条覆盖正常流程/边界场景/异常操作/权限校验/数据安全/库一致性，全部通过。
- 前端 2 个新页面浏览器实测全部生效（日历月/周视图、四状态色块、详情弹窗、快速预约冲突检测、看板三图、时间筛选）。
- 交付代码可直接运行验证（后端 8080、前端 5173 均在本次测试中实际启动运行）。