# R6 轮次测试报告 —— 联调优化（13 页闭环 / 边界修复 / 回归保障）

## 一、测试环境与前置

| 项 | 值 |
| --- | --- |
| 操作系统 | Windows（PowerShell 5.1） |
| 后端 | Spring Boot 3.2.10 / JDK 21 / MyBatis-Plus 3.5.7，运行于 http://localhost:8080 |
| 前端 | Vue 3.4.38 / Vite 5.4.21 / Element Plus 2.7.8 / ECharts 5.5.1（按需引入）/ FullCalendar 6.1.15，运行于 http://localhost:5173 |
| 数据库 | Docker MySQL 8.0（reservation-mysql，库 reservation，root/root，utf8mb4） |
| 基线 | R1 21 + R2 50 + R3 54 + R4 28 + R5 27 = 180 条既有用例（T01-T182）+ R6 新增 30 条（T183-T212）= **210 条** |
| 测试脚本 | `docs/test-script.ps1`（复用 R5 脚本并追加 R6 段，1282 行，可重复执行，结束后恢复初始基线） |
| 初始基线 | 用户 5 / 教室 12 / 预约 13（待审核3·已通过6·已驳回2·已取消2）/ 收藏 6 / ai_config 5（ai.enable=false） |

## 二、总结果

```
用例总数: 210   通过: 210   失败: 0
```

- 覆盖分类（R1-R6 合计）：正常流程 84 / 边界场景 54 / 异常操作 38 / 权限校验 33 / 数据安全 3 / 库一致性 5（库一致性/数据安全归入对应统计口径，与 R5 报告口径一致）。
- R6 新增 30 条分类：正常流程 12 / 边界场景 5 / 异常操作 5 / 权限校验 4 / 数据安全 2 / 库一致性 2。
- 脚本可复跑验证：首轮运行 9 条导出用例失败（PS 5.1 `Invoke-WebRequest -OutFile` 返回对象无 Headers、`$sql200 + 1` 字符串拼接），修复辅助函数后**第二轮全量重跑 210/210 通过**，结束后数据库恢复初始基线（脚本清理段已复核：users=5 / classrooms=12 / reservations=13（3/6/2/2）/ favorites=6 / ai_config=5，临时用户 0 残留）。

## 三、R1-R5 基线回归（T01-T182，共 180 条，全部通过）

复用 R5 交付目录 `docs/test-script.ps1`（T01-T182）并在其上追加 R6 段，回归结果与 R5 报告一致：

| 轮次 | 用例数 | 结果 |
| --- | --- | --- |
| R1 基础（认证/注册/统一返回/全局异常） | 21 | 全部通过 |
| R2 用户管理 | 50 | 全部通过 |
| R2 教室管理（含删除保护/批量状态） | — | 全部通过 |
| R3 预约核心（冲突检测/提交/审核/取消/批量/我的预约） | 54 | 全部通过 |
| R4 体验升级（个人中心/收藏/体验细节） | 28 | 全部通过 |
| R5 亮点功能（日历区间/看板统计） | 27 | 全部通过 |

关键回归锚点（与 R1-R5 验收一致）：冲突检测四类边界、状态流转 0/1/2/3、开始前 1 小时取消限制、统计口径与库一致、日历登录即可 / stats 管理员权限、列表与详情接口不含 password。

## 四、R6 新增用例明细（T183-T212，共 30 条，全部通过）

### 1. 预约记录页 manage 接口（R6 classroomId 教室筛选）

| 编号 | 用例名称 | 类别 | 结果 |
| --- | --- | --- | --- |
| T183 | 按教室筛选（A101=2 条） | 正常流程 | 通过 |
| T184 | 按教室筛选（B101=1 条已通过） | 正常流程 | 通过 |
| T185 | 教室+状态组合（A101 待审核=2） | 正常流程 | 通过 |
| T186 | 教室+日期组合（A101 9/11=2） | 正常流程 | 通过 |
| T187 | 教室+关键词组合（A101+zhangsan=1） | 正常流程 | 通过 |
| T188 | 空数据（A101 已取消=0，返回空 records） | 边界场景 | 通过 |
| T189 | 教室不存在（400「教室不存在」） | 异常操作 | 通过 |
| T190 | 教室参数非数字 classroomId=abc（400「参数类型不正确」） | 异常操作 | 通过 |
| T191 | 日期倒序（400「开始日期不能晚于结束日期」） | 异常操作 | 通过 |
| T192 | 超大分页 size>500（400「每页条数必须在 1-500 之间」） | 边界场景 | 通过 |
| T193 | 页码 0（400「页码必须大于等于 1」） | 边界场景 | 通过 |
| T194 | 分页上限 size=500（200，total=13） | 边界场景 | 通过 |
| T195 | 学生访问 manage（403） | 权限校验 | 通过 |
| T196 | 未登录访问 manage（401） | 权限校验 | 通过 |

### 2. 预约记录导出（GET /api/reservation/export，管理员）

| 编号 | 用例名称 | 类别 | 结果 |
| --- | --- | --- | --- |
| T197 | 全量导出 xlsx（14 行 = 表头 + 13 条，type=xlsx，size=5118） | 正常流程 | 通过 |
| T198 | 状态筛选导出（已通过 6 条 = 7 行） | 正常流程 | 通过 |
| T199 | 教室筛选导出（A101 2 条 = 3 行） | 正常流程 | 通过 |
| T200 | 关键词筛选与库一致（导出 5 行 / SQL 4 条，ok） | 库一致性 | 通过 |
| T201 | 已通过行数与库一致（导出 7 行 / SQL 6 条，ok） | 库一致性 | 通过 |
| T202 | 内容含状态文案与教室（ok） | 正常流程 | 通过 |
| T203 | 空结果仅表头（1 行） | 边界场景 | 通过 |
| T204 | 非法日期参数（code=400） | 异常操作 | 通过 |
| T205 | 教室不存在（code=400） | 异常操作 | 通过 |
| T206 | 学生访问（403） | 权限校验 | 通过 |
| T207 | 未登录访问（401） | 权限校验 | 通过 |
| T208 | 导出内容不含 password（noPwd=True） | 数据安全 | 通过 |
| T209 | 合法 xlsx 文件头（PK 签名，size=5118） | 正常流程 | 通过 |

### 3. 13 页闭环回归锚点与数据安全

| 编号 | 用例名称 | 类别 | 结果 |
| --- | --- | --- | --- |
| T210 | 预约记录字段完整性（13 页闭环数据锚点） | 正常流程 | 通过 |
| T211 | manage 不含 password | 数据安全 | 通过 |
| T212 | 闭环回归-我的预约 zhangsan=4 条 | 正常流程 | 通过 |

## 五、浏览器实测（13 页闭环 + 跨角色流程）

浏览器实测（http://localhost:5173）全部通过，详见 `docs/task-description.md` 第四节：

- **13 页全部可达、可操作、无 404/500、无控制台报错**（仅 favicon 404 噪音，判定无害）：
  登录 / 注册 / 学生端（教室列表 / 教室详情 / 我的预约 / 日历总览 / 个人中心）/ 管理端（首页 / 用户管理 / 教室资源管理 / 预约审核 / 预约记录 / 数据看板）。
- **跨角色闭环实测通过**：学生提交预约（实时冲突校验「该时段可预约」→ 提交成功）→ 管理员审核通过（二次确认）→ 学生端查看状态「已通过」→ 取消（未来预约取消成功；已过时段预约被业务规则正确拦截并友好提示）。
- **边界实测**：登录提醒、退出登录二次确认、审核/取消二次确认、空状态、超大分页、导出下载（中文文件名+时间戳）均正常。

## 六、本轮修复点记录（联调发现的既有缺陷）

| 编号 | 问题 | 修复 | 验证 |
| --- | --- | --- | --- |
| F1 | `classroomId=abc` 参数类型不匹配返回 500 | `GlobalExceptionHandler` 新增 `MethodArgumentTypeMismatchException` → 400「参数类型不正确：classroomId」 | T190 通过 |
| F2 | Dashboard chunk 过大（ECharts 全量约 1MB，build 警告） | echarts 按需引入 + manualChunks 分包 | `npm run build` 无警告；页面实测图表渲染正常 |
| F3 | 管理端首页欢迎文案仍为「模块将在后续迭代上线」（R1 遗留） | 更新为全部模块可用文案（纯文案） | Vite 重新服务后验证 |
| F4 | 收藏/取消按钮快速连续点击可重复提交 | 前端 loading 防抖 + 确认弹窗期间禁用 | 页面实测无重复提交 |

## 七、遗留问题

无。AI 模块（R7 范畴）未实现，`ai.enable=false` 保持关闭。
